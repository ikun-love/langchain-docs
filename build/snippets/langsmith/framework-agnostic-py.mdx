LangSmith 部署支持部署一个 [LangGraph](/oss/python/langgraph/overview) 图（_graph_）。然而，图的节点（_node_）的实现可以包含任意 Python 代码。这意味着任何框架都可以实现在节点内部，并在 LangSmith 部署上进行部署。这使您能够将核心应用逻辑保留在 LangGraph 之外，同时仍能利用 LangSmith 进行[部署](/langsmith/deployments)、扩展和[可观测性](/langsmith/observability)方面的功能。