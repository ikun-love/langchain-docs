LangSmith 部署支持部署一个 [LangGraph](/oss/javascript/langgraph/overview) _图_（graph）。然而，图中任一 _节点_（node）的实现可以包含任意 Python 代码。这意味着任何框架都可以被实现在一个节点内部，并在 LangSmith 部署上运行。这使得您可以将核心应用逻辑保留在 LangGraph 之外，同时仍然利用 LangSmith 进行 [部署](/langsmith/deployments)、扩展和 [可观测性](/langsmith/observability) 方面的工作。