```markdown
---
title: 将追踪数据记录到特定项目
sidebarTitle: 将追踪数据记录到特定项目
---

您可以通过**环境变量（静态方式）**和**运行时动态设置**这两种方式来更改追踪数据的目标项目。

## 静态设置目标项目

如 [追踪概念](/langsmith/observability-concepts#projects) 部分所述，LangSmith 使用 `Project`（项目）的概念来组织追踪数据（traces）。如果未指定项目，默认项目会是 `default`。您可以通过设置 `LANGSMITH_PROJECT` 环境变量来为整个应用程序的运行配置一个自定义的项目名称。这需要在执行应用程序**之前**完成设置。

```
export LANGSMITH_PROJECT=my-custom-project
```

<Warning>
`LANGSMITH_PROJECT` 标志仅在 JS SDK 版本 >= 0.2.16 中受支持。如果使用旧版本，请改用 `LANGCHAIN_PROJECT`。
</Warning>

如果指定的目标项目不存在，当第一条追踪数据被摄入时，该项目将被自动创建。

## 动态设置目标项目

根据您 [如何为追踪数据标注代码](/langsmith/annotate-code) 的方式，您也可以在程序**运行时**通过多种方式动态设置项目名称。当您希望在同一个应用程序中将追踪数据记录到不同的项目时，这种方式非常有用。

<Note>
使用下面任一方法动态设置项目名称，都会覆盖通过 `LANGSMITH_PROJECT` 环境变量设置的项目名称。
</Note>

<CodeGroup>

```python Python
import openai
from langsmith import traceable
from langsmith.run_trees import RunTree

client = openai.Client()
messages = [
  {"role": "system", "content": "You are a helpful assistant."},
  {"role": "user", "content": "Hello!"}
]

# 使用带 'project_name' 参数的 @traceable 装饰器将追踪数据记录到 LangSmith
# 确保已设置 LANGSMITH_TRACING 环境变量以便 @traceable 正常工作
@traceable(
  run_type="llm",
  name="OpenAI Call Decorator",
  project_name="My Project"
)
def call_openai(
  messages: list[dict], model: str = "gpt-4o-mini"
) -> str:
  return client.chat.completions.create(
      model=model,
      messages=messages,
  ).choices[0].message.content

# 调用被装饰的函数
call_openai(messages)

# 您也可以通过 project_name 参数指定项目
# 这将覆盖 @traceable 装饰器中设置的 project_name
call_openai(
  messages,
  langsmith_extra={"project_name": "My Overridden Project"},
)

# 包装后的 OpenAI 客户端接受所有与 @traceable 装饰函数相同的 langsmith_extra 参数，
# 并自动将追踪数据记录到 LangSmith。
# 确保已设置 LANGSMITH_TRACING 环境变量以便包装器正常工作。
from langsmith import wrappers
wrapped_client = wrappers.wrap_openai(client)
wrapped_client.chat.completions.create(
  model="gpt-4o-mini",
  messages=messages,
  langsmith_extra={"project_name": "My Project"},
)

# 或者，创建一个 RunTree 对象
# 您可以使用 project_name 参数设置项目名称
rt = RunTree(
  run_type="llm",
  name="OpenAI Call RunTree",
  inputs={"messages": messages},
  project_name="My Project"
)
chat_completion = client.chat.completions.create(
  model="gpt-4o-mini",
  messages=messages,
)
# 结束并提交运行
rt.end(outputs=chat_completion)
rt.post()
```

```typescript TypeScript
import OpenAI from "openai";
import { traceable } from "langsmith/traceable";
import { wrapOpenAI } from "langsmith/wrappers";
import { RunTree} from "langsmith";

const client = new OpenAI();
const messages = [
  {role: "system", content: "You are a helpful assistant."},
  {role: "user", content: "Hello!"}
];

const traceableCallOpenAI = traceable(async (messages: {role: string, content: string}[], model: string) => {
  const completion = await client.chat.completions.create({
      model: model,
      messages: messages,
  });
  return completion.choices[0].message.content;
},{
  run_type: "llm",
  name: "OpenAI Call Traceable",
  project_name: "My Project"
});

// 调用可追踪函数
await traceableCallOpenAI(messages, "gpt-4o-mini");

// 创建并使用 RunTree 对象
const rt = new RunTree({
  run_type: "llm",
  name: "OpenAI Call RunTree",
  inputs: { messages },
  project_name: "My Project"
});
await rt.postRun();

// 执行聊天补全并在 RunTree 中处理它
rt.end({outputs: chatCompletion});
await rt.patchRun();
```

</CodeGroup>
```

---

<Callout icon="pen-to-square" iconType="regular">
    [Edit the source of this page on GitHub.](https://github.com/langchain-ai/docs/edit/main/src/langsmith\log-traces-to-project.mdx)
</Callout>
<Tip icon="terminal" iconType="regular">
    [Connect these docs programmatically](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Tip>
