---
title: 混合部署
sidebarTitle: 概览
---

<Info>
**重要提示**
混合部署模式需要 [企业版](https://langchain.com/pricing) 计划。
</Info>

**混合**部署将 LangSmith 基础设施分割到 LangChain 的云环境和您自己的云环境中：

- **控制平面**（Control plane，即 LangSmith UI、API 和编排服务）在 LangChain 的云中运行，由 LangChain 管理。
- **数据平面**（Data plane，即您的 <Tooltip tip="运行您的应用程序的服务器">Agent Servers</Tooltip> 和代理工作负载）在您的云中运行，由您自己管理。

这结合了托管界面的便利性与在您自己的环境中运行工作负载的灵活性。

<Note>
了解更多关于 [控制平面](/langsmith/control-plane)、[数据平面](/langsmith/data-plane) 和 [Agent Server](/langsmith/agent-server) 架构概念的信息。
</Note>

| 组件 | 职责 | 运行位置 | 管理方 |
|---|---|---|---|
| <Tooltip tip="用于管理部署的 LangSmith UI 和 API">控制平面</Tooltip> | <ul><li>用于创建部署和版本的 UI</li><li>用于管理部署的 API</li><li>可观测性数据存储</li></ul> | LangChain 的云 | LangChain |
| <Tooltip tip="Agent Servers 和代理执行的运行时环境">数据平面</Tooltip> | <ul><li>用于协调部署的 Operator/监听器</li><li>Agent Servers（代理/图）</li><li>后端服务（如 Postgres、Redis 等）</li></ul> | 您的云 | 您 |

当以混合模式运行 LangSmith 时，您需要使用 [LangSmith API 密钥](/langsmith/create-account-api-key) 进行身份验证。

### 工作流程

1. 使用 `langgraph-cli` 或 [Studio](/langsmith/studio) 在本地测试您的图（graph）。
1. 使用 `langgraph build` 命令构建 Docker 镜像。
1. 从 [控制平面 UI](/langsmith/control-plane#control-plane-ui) 部署您的 Agent Server。

<Note>
支持的计算平台：[Kubernetes](https://kubernetes.io/)。<br></br>
关于设置，请参阅 [混合部署设置指南](/langsmith/deploy-hybrid)。
</Note>

### 架构

<img
    className="block dark:hidden"
    src="/langsmith/images/hybrid-with-deployment-light.png"
    alt="混合部署：LangChain 托管的控制平面（LangSmith UI/API）管理部署。您的云在 Kubernetes 上运行一个监听器、Agent Server 实例和后端存储（Postgres/Redis）。"
/>

<img
    className="hidden dark:block"
    src="/langsmith/images/hybrid-with-deployment-dark.png"
    alt="混合部署：LangChain 托管的控制平面（LangSmith UI/API）管理部署。您的云在 Kubernetes 上运行一个监听器、Agent Server 实例和后端存储（Postgres/Redis）。"
/>

### 计算平台

- **Kubernetes**: 混合模式支持在任何 Kubernetes 集群上运行数据平面。

<Tip>
有关在 Kubernetes 中的设置，请参阅 [混合部署设置指南](/langsmith/deploy-hybrid)
</Tip>

### 到 LangSmith 和控制平面的出站通信 (Egress)

在混合部署模型中，您自托管的数据平面将向控制平面发送网络请求，以轮询需要在数据平面中实施的更改。来自数据平面部署的追踪信息也会发送到与控制平面集成的 LangSmith 实例。到控制平面的此流量是加密的，通过 HTTPS 传输。数据平面使用 LangSmith API 密钥向控制平面进行身份验证。

为了启用此出站流量，您可能需要更新内部防火墙规则或云资源（如安全组），以 [允许特定的 IP 地址](/langsmith/cloud#ingress-into-langchain-saas)。

<Warning>
目前不支持 AWS/Azure PrivateLink 或 GCP Private Service Connect。此流量将通过公共互联网传输。
</Warning>

## 监听器 (Listeners)

在混合部署选项中，可以运行一个或多个 ["监听器" 应用程序](/langsmith/data-plane#”listener”-application)，具体取决于您的 LangSmith 工作区和 Kubernetes 集群的组织方式。

### Kubernetes 集群组织
- 一个 Kubernetes 集群中可以运行一个或多个监听器。
- 一个监听器可以部署到一个或多个命名空间中。
- 多个监听器不能部署到同一个命名空间。
- 集群所有者负责规划监听器的布局和 Agent Server 的部署。

### LangSmith 工作区组织
- 一个工作区可以关联一个或多个监听器。
- 一个监听器只能关联到一个工作区。LangSmith 工作区到监听器是一个一对多的关系。
- 一个工作区只能部署到部署了其所有监听器的那些 Kubernetes 集群上。

## 使用场景

以下是一些常见的监听器配置（并非严格要求）：

### 每个 LangSmith 工作区 → 独立的 Kubernetes 集群
- 集群 `alpha` 运行工作区 `A`
- 集群 `beta` 运行工作区 `B`

### 单个集群，每个工作区一个命名空间
- 集群 `alpha`，命名空间 `1` 运行工作区 `A`
- 集群 `alpha`，命名空间 `2` 运行工作区 `B`

### 独立的集群，共享一个 “开发” 集群
- 集群 `alpha` 运行工作区 `A`
- 集群 `beta` 运行工作区 `B`
- 集群 `dev` 运行工作区 `A` 和 `B`
- 两个工作区都有两个监听器；集群 `dev` 进行了两次监听器部署

---

<Callout icon="pen-to-square" iconType="regular">
    [Edit the source of this page on GitHub.](https://github.com/langchain-ai/docs/edit/main/src/langsmith\hybrid.mdx)
</Callout>
<Tip icon="terminal" iconType="regular">
    [Connect these docs programmatically](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Tip>
