---
title: 如何在一个评估器中返回多个分数
sidebarTitle: 在一个评估器中返回多个分数
---

有时，自定义评估器或汇总评估器返回多个指标会很有用。例如，如果您有一个 LLM 判官生成了多个指标，那么通过**一次 LLM 调用**生成多个指标，而不是进行多次 LLM 调用，可以节省时间和金钱。

要使用 Python SDK 返回多个分数，只需返回一个包含以下形式的字典/对象的列表：

```python
[
    # 'key' 是指标的名称 (metric name)
    # 'score' 是一个数值指标的值
    {"key": string, "score": number},
    # 'value' 是一个分类指标的值
    {"key": string, "value": string},
    ... # 您可以记录任意数量的指标
]
```

要使用 JS/TS SDK 实现此功能，请返回一个包含 `'results'` 键的对象，该键的值是一个上述形式的列表：

```typescript
{results: [{ key: string, score: number }, ...]};
```

这些字典中的每一个都可以包含 [反馈字段](/langsmith/feedback-data-format) 中的任意或所有字段；请查阅链接文档以获取更多信息。

示例：

- Python：需要 `langsmith>=0.2.0`
- TypeScript：`langsmith@0.1.32` 及更高版本支持多个分数

<CodeGroup>

```python Python
def multiple_scores(outputs: dict, reference_outputs: dict) -> list[dict]:
    # 替换为实际的评估逻辑。
    precision = 0.8
    recall = 0.9
    f1 = 0.85
    return [
        {"key": "precision", "score": precision},
        {"key": "recall", "score": recall},
        {"key": "f1", "score": f1},
    ]
```

```typescript TypeScript
import type { Run, Example } from "langsmith/schemas";

function multipleScores(rootRun: Run, example: Example) {
  // 您的评估逻辑
  return {
      results: [
          { key: "precision", score: 0.8 },
          { key: "recall", score: 0.9 },
          { key: "f1", score: 0.85 },
      ],
  };
}
```

</CodeGroup>

最终实验结果的表格行将显示每个分数。

![multiple_scores.png](/langsmith/images/multiple-scores.png)

## 相关

* [返回分类指标与数值指标](/langsmith/metric-type)

---

<Callout icon="pen-to-square" iconType="regular">
    [Edit the source of this page on GitHub.](https://github.com/langchain-ai/docs/edit/main/src/langsmith\multiple-scores.mdx)
</Callout>
<Tip icon="terminal" iconType="regular">
    [Connect these docs programmatically](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Tip>
