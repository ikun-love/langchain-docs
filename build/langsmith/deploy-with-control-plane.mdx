```md
---
title: 使用控制平面进行部署
sidebarTitle: 使用控制平面
icon: "cloud-arrow-up"
---

本指南将向您展示如何使用 [控制平面](/langsmith/control-plane)，将应用程序部署到 [混合环境](/langsmith/hybrid) 或 [自托管环境](/langsmith/self-hosted) 实例上。有了控制平面，您可以在本地构建 Docker 镜像，将其推送到您的 Kubernetes 集群可以访问的镜像仓库（Registry），然后使用 [LangSmith UI](https://smith.langchain.com) 进行部署。

<Note>
**本指南面向应用程序部署，而非基础设施设置。**

在使用本指南之前，您必须已经完成了基础设施的设置工作：
- **[混合环境设置](/langsmith/deploy-hybrid)**：适用于混合托管场景。
- **[启用 LangSmith 部署](/langsmith/deploy-self-hosted-full-platform)**：适用于启用了控制平面的自托管场景。

如果您尚未设置基础设施，请从 [平台设置部分](/langsmith/platform-setup) 开始。
</Note>

## 概述

通过控制平面部署到混合或自托管 LangSmith 实例的应用程序使用 Docker 镜像。在本指南中，应用程序的部署工作流程如下：

1. 使用 `langgraph dev` 或 [Studio](/langsmith/studio) 在本地测试您的应用程序。
1. 使用 `langgraph build` 命令构建一个 Docker 镜像。
1. 将该镜像推送到您的基础设施可以访问的容器注册中心。
1. 在 [控制平面 UI](/langsmith/control-plane#control-plane-ui) 中指定镜像 URL 来进行部署。

## 前提条件

在完成本指南之前，您需要满足以下条件：

- 已完成基础设施设置，以确保您的 [数据平面](/langsmith/data-plane) 能够接收应用程序部署：
  - [混合环境设置](/langsmith/deploy-hybrid)：在您的 Kubernetes 集群中安装数据平面组件（监听器、操作员、CRD），这些组件将连接到 LangChain 的托管控制平面。
  - [启用 LangSmith 部署](/langsmith/deploy-self-hosted-full-platform)：在您的自托管 LangSmith 实例上启用 LangSmith Deployment 功能。
- 可以访问已启用 LangSmith Deployment 功能的 [LangSmith UI](https://smith.langchain.com)。
- 您的 Kubernetes 集群可以访问的容器注册中心。如果使用需要身份验证的私有注册中心，则必须在基础设施设置过程中配置镜像拉取密钥。请参阅 [私有注册中心身份验证](#private-registry-authentication)。

## 步骤 1. 本地测试

在部署之前，请在本地测试您的应用程序。您可以使用 [LangGraph CLI](/langsmith/cli#dev) 以开发模式运行 Agent 服务器：

```bash
langgraph dev
```

有关本地测试的完整指南，请参阅 [本地服务器快速入门](/langsmith/local-server)。

## 步骤 2. 构建 Docker 镜像

使用 [`langgraph build`](/langsmith/cli#build) 命令构建应用程序的 Docker 镜像：

```bash
langgraph build -t my-image
```

构建命令的选项包括：

| 选项 | 默认值 | 描述 |
|--------|---------|-------------|
| `-t, --tag TEXT` | 必需 | Docker 镜像的标签 (Tag) |
| `--platform TEXT` | | 要构建的目标平台（例如：`linux/amd64,linux/arm64`） |
| `--pull / --no-pull` | `--pull` | 使用最新的远程 Docker 镜像进行构建 |
| `-c, --config FILE` | `langgraph.json` | 配置文件路径 |

指定平台的示例：

```bash
langgraph build --platform linux/amd64 -t my-image:v1.0.0
```

有关完整详情，请参阅 [CLI 参考](/langsmith/cli#build)。

## 步骤 3. 推送到容器注册中心

将您的镜像推送到您的 Kubernetes 集群可以访问的容器注册中心。具体命令取决于您的注册中心提供商。

<Tip>
请为您的镜像添加版本信息作为标签（例如 `my-registry.com/my-app:v1.0.0`），以便于回滚操作。
</Tip>

## 步骤 4. 使用控制平面 UI 进行部署

[控制平面 UI](/langsmith/control-plane#control-plane-ui) 允许您创建和管理部署、查看日志和指标以及更新配置。要在 [LangSmith UI](https://smith.langchain.com) 中创建新部署：

1. 在左侧导航面板中，选择 **Deployments**（部署）。
1. 在右上角，选择 **+ New Deployment**（新建部署）。
1. 在部署配置面板中，提供以下信息：
   - **Image URL**：您在 [步骤 3](#step-3-push-to-container-registry) 中推送的完整镜像 URL。
   - **Listener/Compute ID**：选择已为您的基础设施配置的监听器。
   - **Namespace**：要部署到的 Kubernetes 命名空间。
   - **Environment variables**：任何必需的配置（API 密钥等）。
   - 根据需要调整其他部署设置。
1. 选择 **Submit**（提交）。

控制平面将与您的 [数据平面](/langsmith/data-plane) 监听器协调以部署您的应用程序。

创建部署后，基础设施将 [异步配置资源](/langsmith/control-plane#asynchronous-deployment)。部署可能需要几分钟时间，首次部署由于涉及数据库创建，耗时可能更长。

从控制平面 UI，您可以查看构建日志、服务器日志和部署指标，包括 CPU/内存使用率、副本数和 API 性能。有关更多详细信息，请参阅 [控制平面监控文档](/langsmith/control-plane#monitoring)。

<Note>
控制平面会自动为每个部署创建一个同名的 [LangSmith 观测追踪项目](/langsmith/observability)。追踪环境变量也由控制平面自动设置。
</Note>

## 更新部署

要部署应用程序的新版本，请创建一个 [新修订版（Revision）](/langsmith/control-plane#revisions)：

从 LangSmith UI 开始：

1. 在左侧导航面板中，选择 **Deployments**（部署）。
1. 选择一个现有部署。
1. 在部署视图中，选择右上角的 **+ New Revision**（新建修订版）。
1. 更新配置：
   - 将 **Image URL** 更新为您新的镜像版本。
   - 如果需要，更新环境变量。
   - 根据需要调整其他设置。
1. 选择 **Submit**（提交）。

## 私有注册中心身份验证

如果您的容器注册中心需要身份验证（例如 AWS ECR、Azure ACR、GCP Artifact Registry、私有 Docker 注册中心），则必须在部署应用程序之前配置 Kubernetes 镜像拉取密钥。**这是仅需配置一次的基础设施操作。**

<Note>
**此配置在基础设施级别完成，而非针对每个部署单独配置。** 一旦配置完成，所有部署都会自动继承该注册表的凭据。
</Note>

配置步骤取决于您的部署类型：

- **带控制平面的自托管环境**：在 LangSmith Helm chart 的 `values.yaml` 文件中配置 `imagePullSecrets`。请参阅 [启用 LangSmith 部署指南](/langsmith/deploy-self-hosted-full-platform#setup) 中的详细步骤。
- **混合环境**：使用相同的格式，在您的 `langgraph-dataplane-values.yaml` 文件中配置 `imagePullSecrets`。

有关针对不同注册中心提供商创建镜像拉取密钥的详细步骤，请参阅 [Kubernetes 关于从私有注册中心拉取镜像的任务文档](https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/)。

## 后续步骤

- **[控制平面](/langsmith/control-plane)**：了解更多关于控制平面的功能。
- **[数据平面](/langsmith/data-plane)**：了解数据平面的架构。
- **[观测性 (Observability)](/langsmith/observability)**：通过自动追踪来监控您的部署。
- **[Studio](/langsmith/studio)**：测试和调试已部署的应用程序。
- **[LangGraph CLI](/langsmith/cli)**：完整的 CLI 参考文档。
```

---

<Callout icon="pen-to-square" iconType="regular">
    [Edit the source of this page on GitHub.](https://github.com/langchain-ai/docs/edit/main/src/langsmith\deploy-with-control-plane.mdx)
</Callout>
<Tip icon="terminal" iconType="regular">
    [Connect these docs programmatically](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Tip>
