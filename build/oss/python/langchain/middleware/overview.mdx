---
title: 概述
description: 在智能体执行的每一步控制和自定义其行为
---

中间件（Middleware）提供了一种方式，可以更紧密地控制智能体内部发生的操作。中间件在以下方面非常有用：

- 使用日志记录（logging）、分析（analytics）和调试（debugging）来跟踪智能体的行为。
- 转换提示词（prompts）、[工具选择](/oss/python/langchain/middleware/built-in#llm-tool-selector)和输出格式化。
- 添加[重试机制](/oss/python/langchain/middleware/built-in#tool-retry)、[回退（fallbacks）策略](/oss/python/langchain/middleware/built-in#model-fallback)和提前终止逻辑。
- 应用[速率限制](/oss/python/langchain/middleware/built-in#model-call-limit)、护栏（guardrails）和[个人身份信息（PII）检测](/oss/python/langchain/middleware/built-in#pii-detection)。

通过将中间件传递给 [`create_agent`](https://reference.langchain.com/python/langchain/agents/#langchain.agents.create_agent) 来添加它们：

```python
from langchain.agents import create_agent
from langchain.agents.middleware import SummarizationMiddleware, HumanInTheLoopMiddleware

agent = create_agent(
    model="gpt-4o",
    tools=[...],
    middleware=[
        SummarizationMiddleware(...),
        HumanInTheLoopMiddleware(...)
    ],
)
```




## 智能体循环 (The agent loop)

核心的智能体循环涉及调用模型（model），允许它选择要执行的工具（tools），然后在它不再调用任何工具时结束：

<img
    src="/oss/images/core_agent_loop.png"
    alt="核心智能体循环图示"
    style={{height: "200px", width: "auto", justifyContent: "center"}}
    className="rounded-lg block mx-auto"
/>

中间件在这些步骤的每一次调用之前和之后暴露了钩子（hooks）：

<img
    src="/oss/images/middleware_final.png"
    alt="中间件流程图"
    style={{height: "300px", width: "auto", justifyContent: "center"}}
    className="rounded-lg mx-auto"
/>

## 附加资源 (Additional resources)

<CardGroup cols={2}>
    <Card title="内置中间件" icon="box" href="/oss/python/langchain/middleware/built-in">
        探索用于常见用例的内置中间件。
    </Card>
    <Card title="自定义中间件" icon="code" href="/oss/python/langchain/middleware/custom">
        使用钩子和装饰器构建您自己的中间件。
    </Card>
    <Card title="中间件 API 参考文档" icon="book" href="https://reference.langchain.com/python/langchain/middleware/">
        中间件的完整 API 参考。
    </Card>
    <Card title="测试智能体" icon="scale-unbalanced" href="/oss/python/langchain/test">
        使用 LangSmith 测试您的智能体。
    </Card>
</CardGroup>

---

<Callout icon="pen-to-square" iconType="regular">
    [Edit the source of this page on GitHub.](https://github.com/langchain-ai/docs/edit/main/src/oss\langchain\middleware\overview.mdx)
</Callout>
<Tip icon="terminal" iconType="regular">
    [Connect these docs programmatically](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Tip>
