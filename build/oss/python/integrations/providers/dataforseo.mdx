---
title: DataForSEO
---

>[DataForSeo](https://dataforseo.com/) 通过 API 提供全面的 SEO 和数字营销数据解决方案。

本页面提供了如何在 LangChain 中使用 DataForSEO 搜索 API 的说明。

## 安装与设置

获取 [DataForSEO API 访问的登录名和密码](https://app.dataforseo.com/register)，并将它们设置为环境变量（分别为 `DATAFORSEO_LOGIN` 和 `DATAFORSEO_PASSWORD`）。

```python
import os

os.environ["DATAFORSEO_LOGIN"] = "your_login"
os.environ["DATAFORSEO_PASSWORD"] = "your_password"
```


## 工具类 (Utility)

`DataForSEO` 工具类封装了这个 API。要导入此类，请使用：

```python
from langchain_community.utilities.dataforseo_api_search import DataForSeoAPIWrapper
```

有关此类包装器的详细演练，请参阅 [此 Notebook](/oss/python/integrations/tools/dataforseo)。

## 工具 (Tool)

您也可以将此类包装器加载为一个 Tool，以便与 Agent 一起使用：

```python
from langchain_community.agent_toolkits.load_tools import load_tools
tools = load_tools(["dataforseo-api-search"])
```

这将加载以下工具：

```python
from langchain_community.tools import DataForSeoAPISearchRun
from langchain_community.tools import DataForSeoAPISearchResults
```

## 示例用法

```python
dataforseo = DataForSeoAPIWrapper(api_login="your_login", api_password="your_password")
result = dataforseo.run("Bill Gates")
print(result)
```

---

<Callout icon="pen-to-square" iconType="regular">
    [Edit the source of this page on GitHub.](https://github.com/langchain-ai/docs/edit/main/src/oss\python\integrations\providers\dataforseo.mdx)
</Callout>
<Tip icon="terminal" iconType="regular">
    [Connect these docs programmatically](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Tip>
