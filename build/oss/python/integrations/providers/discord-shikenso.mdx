---
title: Discord
---

> [Discord](https://discord.com/) 是一个即时消息、语音和视频通信平台，被各类社区广泛使用。

## 安装与设置

安装 `langchain-discord-shikenso` 包：

<CodeGroup>
```bash pip
pip install langchain-discord-shikenso
```

```bash uv
uv add langchain-discord-shikenso
```
</CodeGroup>

您必须通过环境变量提供一个机器人令牌（bot token），以便工具能够通过 Discord API 进行身份验证：

```bash
export DISCORD_BOT_TOKEN="your-discord-bot-token"
```

如果未设置 `DISCORD_BOT_TOKEN` 环境变量，实例化工具时将会抛出 `ValueError` 错误。

---

## 工具 (Tools)

下面的代码片段展示了如何在 Discord 中读取和发送消息。更多细节，请参阅 [Discord 工具的文档](/oss/python/integrations/tools/discord)。

```python
from langchain_discord.tools.discord_read_messages import DiscordReadMessages
from langchain_discord.tools.discord_send_messages import DiscordSendMessage

# 创建工具实例
read_tool = DiscordReadMessages()
send_tool = DiscordSendMessage()

# 示例：从频道 1234567890 读取最近 3 条消息
read_result = read_tool({"channel_id": "1234567890", "limit": 3})
print(read_result)

# 示例：向频道 1234567890 发送一条消息
send_result = send_tool({"channel_id": "1234567890", "message": "Hello from Markdown example!"})
print(send_result)
```

---

## 工具包 (Toolkit)

`DiscordToolkit` 将多个与 Discord 相关的工具组合成一个统一的接口。有关使用示例，请参见 [Discord 工具包文档](/oss/python/integrations/tools/discord)。

```python
from langchain_discord.toolkits import DiscordToolkit

toolkit = DiscordToolkit()
tools = toolkit.get_tools()

read_tool = tools[0]  # DiscordReadMessages
send_tool = tools[1]  # DiscordSendMessage
```

---

## 未来集成

可以为 Discord 添加更多集成功能（例如文档加载器、聊天加载器）。请查阅 [Discord 开发者文档](https://discord.com/developers/docs/intro) 以获取更多信息，并关注 [langchain\_discord GitHub 仓库](https://github.com/Shikenso-Analytics/langchain-discord) 中的更新或高级用例。

---

<Callout icon="pen-to-square" iconType="regular">
    [Edit the source of this page on GitHub.](https://github.com/langchain-ai/docs/edit/main/src/oss\python\integrations\providers\discord-shikenso.mdx)
</Callout>
<Tip icon="terminal" iconType="regular">
    [Connect these docs programmatically](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Tip>
