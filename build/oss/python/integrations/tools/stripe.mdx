---
title: StripeAgentToolkit
---

本指南提供了开始使用 Stripe 的智能体工具包（agent toolkit）的快速概述。

您可以在 [Stripe 的发布博客](https://stripe.dev/blog/adding-payments-to-your-agentic-workflows) 或该项目的 [PyPi 页面](https://pypi.org/project/stripe-agent-toolkit/) 上阅读更多关于 `StripeAgentToolkit` 的信息。

## 概述

### 集成详情

| 类 (Class) | 包 (Package) | 可序列化 (Serializable) | [JS 支持](https://github.com/stripe/agent-toolkit?tab=readme-ov-file#typescript) | 版本 (Version) |
| :--- | :--- | :---: | :---: | :---: |
| StripeAgentToolkit | [stripe-agent-toolkit](https://pypi.org/project/stripe-agent-toolkit) | ❌ | ✅ | ![PyPI - Version](https://img.shields.io/pypi/v/stripe-agent-toolkit?style=flat-square&label=%20) |

## 设置

这个外部管理的包托管在 `stripe-agent-toolkit` 项目中，该项目由 Stripe 团队管理。

您可以使用 `pip` 安装它，同时为以下示例安装 `langgraph`：

```python
pip install --quiet -U langgraph stripe-agent-toolkit
```

### 凭证

除了安装包之外，您还需要使用 Stripe 账户的密钥来配置集成。该密钥可以在您的 [Stripe Dashboard](https://dashboard.stripe.com/account/apikeys) 中找到。

```python
import getpass
import os

if not os.environ.get("STRIPE_SECRET_KEY"):
    os.environ["STRIPE_SECRET_KEY"] = getpass.getpass("STRIPE API key:\n")
```

为了获得最佳的一流可观测性，设置 [LangSmith](https://smith.langchain.com/) 也很有帮助（但不是必需的）：

```python
os.environ["LANGSMITH_TRACING"] = "true"
# os.environ["LANGSMITH_ API_KEY"] = getpass.getpass()
```

## 实例化

这里展示了如何创建一个 `Stripe Toolkit` 的实例。

```python
from stripe_agent_toolkit.langchain.toolkit import StripeAgentToolkit

stripe_agent_toolkit = StripeAgentToolkit(
    secret_key=os.getenv("STRIPE_SECRET_KEY"),
    configuration={
        "actions": {
            "payment_links": {
                "create": True,
            },
        }
    },
)
```

## 智能体 (Agent)

以下是如何使用该工具包在 langgraph 中创建一个基础智能体：

```python
from langchain_anthropic import ChatAnthropic
from langchain.agents import create_agent


model = ChatAnthropic(
    model="claude-3-5-sonnet-20240620",
)

langgraph_agent_executor = create_agent(model, stripe_agent_toolkit.get_tools())

input_state = {
    "messages": """
        为一款名为 'test' 的新产品创建一个支付链接，价格为 $100。
        并就购买机器人（buy bots）写一首有趣的描述，最好是一首俳句。
    """,
}

output_state = langgraph_agent_executor.invoke(input_state)

print(output_state["messages"][-1].content)
```

---

<Callout icon="pen-to-square" iconType="regular">
    [Edit the source of this page on GitHub.](https://github.com/langchain-ai/docs/edit/main/src/oss\python\integrations\tools\stripe.mdx)
</Callout>
<Tip icon="terminal" iconType="regular">
    [Connect these docs programmatically](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Tip>
