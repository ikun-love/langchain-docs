---
title: MathPixPDFLoader
---

受 Daniel Gross 在此处 Snippet 的启发：[https://gist.github.com/danielgross/3ab4104e14faccc12b49200843adab21](https://gist.github.com/danielgross/3ab4104e14faccc12b49200843adab21)

## 概述 (Overview)

### 集成详情 (Integration details)

| 类 (Class) | 包 (Package) | 本地 (Local) | 可序列化 (Serializable) | JS 支持 (JS support)|
| :--- | :--- | :---: | :---: |  :---: |
| [MathPixPDFLoader](https://python.langchain.com/api_reference/community/document_loaders/langchain_community.document_loaders.pdf.MathpixPDFLoader.html) | [langchain-community](https://python.langchain.com/api_reference/community/index.html) | ✅ | ❌ | ❌ |

### 加载器特性 (Loader features)

| 源 (Source) | 文档延迟加载 (Document Lazy Loading) | 原生异步支持 (Native Async Support)
| :---: | :---: | :---: |
| MathPixPDFLoader | ✅ | ❌ |

## 设置 (Setup)

### 凭证 (Credentials)

请注册 Mathpix 并[创建一个 API 密钥](https://mathpix.com/docs/ocr/creating-an-api-key)，然后将 `MATHPIX_API_KEY` 变量设置到您的环境中。

```python
import getpass
import os

if "MATHPIX_API_KEY" not in os.environ:
    os.environ["MATHPIX_API_KEY"] = getpass.getpass("Enter your Mathpix API key: ")
```

要启用对您的模型调用的自动跟踪，请设置您的 [LangSmith](https://docs.smith.langchain.com/) API 密钥：

```python
os.environ["LANGSMITH_API_KEY"] = getpass.getpass("Enter your LangSmith API key: ")
os.environ["LANGSMITH_TRACING"] = "true"
```

### 安装 (Installation)

安装 **langchain-community**。

```python
pip install -qU langchain-community
```

## 初始化 (Initialization)

现在我们准备初始化加载器：

```python
from langchain_community.document_loaders import MathpixPDFLoader

file_path = "./example_data/layout-parser-paper.pdf"
loader = MathpixPDFLoader(file_path)
```

## 加载 (Load)

```python
docs = loader.load()
docs[0]
```

```python
print(docs[0].metadata)
```

## 延迟加载 (Lazy Load)

```python
page = []
for doc in loader.lazy_load():
    page.append(doc)
    if len(page) >= 10:
        # 执行一些分页操作，例如
        # index.upsert(page)

        page = []
```

---

## API 参考 (API reference)

有关所有 MathpixPDFLoader 特性和配置的详细文档，请参阅 API 参考：[python.langchain.com/api_reference/community/document_loaders/langchain_community.document_loaders.pdf.MathpixPDFLoader.html](https://python.langchain.com/api_reference/community/document_loaders/langchain_community.document_loaders.pdf.MathpixPDFLoader.html)

---

<Callout icon="pen-to-square" iconType="regular">
    [Edit the source of this page on GitHub.](https://github.com/langchain-ai/docs/edit/main/src/oss\python\integrations\document_loaders\mathpix.mdx)
</Callout>
<Tip icon="terminal" iconType="regular">
    [Connect these docs programmatically](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Tip>
