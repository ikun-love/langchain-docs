---
title: Databricks 向量搜索
---


[Databricks 向量搜索 (Vector Search)](https://docs.databricks.com/en/generative-ai/vector-search.html) 是一个无服务器的相似性搜索引擎。它允许您在一个向量数据库中存储数据的向量表示形式，包括元数据。通过向量搜索，您可以基于 Unity Catalog 管理的 Delta 表创建自动更新的向量搜索索引，并通过简单的 API 来查询这些索引，从而返回最相似的向量。

本 Notebook 演示了如何将 LangChain 与 Databricks 向量搜索结合使用。

## 设置 (Setup)

要访问 Databricks 模型，您需要创建一个 Databricks 账户，设置凭据（仅当您在 Databricks 工作区外部运行时才需要），并安装所需的软件包。

### 凭据（仅当您在 Databricks 外部时）

如果您在 Databricks 内部运行 LangChain 应用，则可以跳过此步骤。

否则，您需要手动将 Databricks 工作区主机名和个人访问令牌分别设置为环境变量 `DATABRICKS_HOST` 和 `DATABRICKS_TOKEN`。有关如何获取访问令牌的更多信息，请参阅 [认证文档 (Authentication Documentation)](https://docs.databricks.com/en/dev-tools/auth/index.html#databricks-personal-access-tokens)。

```python
import getpass
import os

os.environ["DATABRICKS_HOST"] = "https://your-databricks-workspace"
if "DATABRICKS_TOKEN" not in os.environ:
    os.environ["DATABRICKS_TOKEN"] = getpass.getpass(
        "Enter your Databricks access token: "
    )
```

### 安装 (Installation)

LangChain 的 Databricks 集成包含在 `databricks-langchain` 包中。

```python
pip install -qU databricks-langchain
```

### 创建向量搜索终结点和索引（如果尚未创建）

在本节中，我们将使用客户端 SDK 创建一个 Databricks 向量搜索终结点 (endpoint) 和一个索引 (index)。

如果您已经拥有一个终结点和一个索引，则可以跳过本节，直接进入“实例化 (Instantiation)”部分。

首先，实例化 Databricks VectorSearch 客户端：

```python
from databricks.vector_search.client import VectorSearchClient

client = VectorSearchClient()
```

接下来，我们将创建一个新的 VectorSearch 终结点。

```python
endpoint_name = "<your-endpoint-name>"

client.create_endpoint(name=endpoint_name, endpoint_type="STANDARD")
```

最后，我们将创建一个可以在该终结点上查询的索引。Databricks 向量搜索中存在两种类型的索引，`DatabricksVectorSearch` 类支持这两种用例。

* **Delta 同步索引 (Delta Sync Index)**：自动与源 Delta 表同步，并在底层 Delta 表中的数据发生变化时，自动且增量地更新索引。

* **直接向量访问索引 (Direct Vector Access Index)**：支持向量和元数据的直接读写。用户需要使用 REST API 或 Python SDK 负责更新此表。

对于 Delta 同步索引，您也可以选择使用 Databricks 管理的嵌入 (Databricks-managed embeddings) 或自管的嵌入（通过 LangChain 嵌入类）。

以下代码创建一个**直接访问**索引。有关创建其他类型索引的说明，请参阅 [Databricks 文档](https://docs.databricks.com/en/generative-ai/create-query-vector-search.html)。

```python
index_name = "<your-index-name>"  # 格式: "<catalog>.<schema>.<index-name>"

index = client.create_direct_access_index(
    endpoint_name=endpoint_name,
    index_name=index_name,
    primary_key="id",
    # 嵌入的维度。请根据您使用的嵌入模型进行更改。
    embedding_dimension=3072,
    # 用于存储文本数据嵌入向量的列
    embedding_vector_column="text_vector",
    schema={
        "id": "string",
        "text": "string",
        "text_vector": "array<float>",
        # 可选的元数据列
        "source": "string",
    },
)

index.describe()
```

## 实例化 (Instantiation)

`DatabricksVectorSearch` 的实例化方式略有不同，具体取决于您的索引使用的是 Databricks 管理的嵌入还是您自选的自管嵌入（即 LangChain 嵌入对象）。

如果您使用的是带有 Databricks 管理的嵌入的 Delta 同步索引：

```python
from databricks_langchain import DatabricksVectorSearch

vector_store = DatabricksVectorSearch(
    endpoint=endpoint_name,
    index_name=index_name,
)
```

如果您使用的是直接访问索引或带有自管嵌入的 Delta 同步索引，则还需要提供源表中的嵌入模型和文本列，以便进行嵌入生成：

<EmbeddingTabs/>

```python
# | output: false
# | echo: false
from langchain_openai import OpenAIEmbeddings

embeddings = OpenAIEmbeddings(model="text-embedding-3-large")
```

```python
vector_store = DatabricksVectorSearch(
    endpoint=endpoint_name,
    index_name=index_name,
    embedding=embeddings,
    # 索引中包含要嵌入的文本数据的列名称
    text_column="document_content",
)
```

## 管理向量存储 (Manage vector store)

### 向向量存储添加项 (Add items to vector store)

注意：通过 `add_documents` 方法向向量存储添加项仅支持**直接访问**索引。

```python
from langchain_core.documents import Document

document_1 = Document(page_content="foo", metadata={"source": "https://example.com"})

document_2 = Document(page_content="bar", metadata={"source": "https://example.com"})

document_3 = Document(page_content="baz", metadata={"source": "https://example.com"})

documents = [document_1, document_2, document_3]

vector_store.add_documents(documents=documents, ids=["1", "2", "3"])
```

```output
['1', '2', '3']
```

### 从向量存储中删除项 (Delete items from vector store)

注意：通过 `delete` 方法从向量存储中删除项仅支持**直接访问**索引。

```python
vector_store.delete(ids=["3"])
```

```output
True
```

## 查询向量存储 (Query vector store)

一旦您的向量存储被创建并且相关的文档已被添加，在运行链或代理时，您很可能希望对其进行查询。

### 直接查询 (Query directly)

如下所示，可以执行简单的相似性搜索：

```python
results = vector_store.similarity_search(
    query="thud", k=1, filter={"source": "https://example.com"}
)
for doc in results:
    print(f"* {doc.page_content} [{doc.metadata}]")
```

```output
* foo [{'id': '1'}]
```

注意：默认情况下，相似性搜索仅返回主键和文本列。如果希望检索与文档关联的自定义元数据，请在初始化向量存储时，通过 `columns` 参数传入额外的列名。

```python
vector_store = DatabricksVectorSearch(
    endpoint=endpoint_name,
    index_name=index_name,
    embedding=embeddings,
    text_column="text",
    columns=["source"],
)

results = vector_store.similarity_search(query="thud", k=1)
for doc in results:
    print(f"* {doc.page_content} [{doc.metadata}]")
```

```output
* foo [{'source': 'https://example.com', 'id': '1'}]
```

如果您想执行相似性搜索并接收相应的分数，可以运行：

```python
results = vector_store.similarity_search_with_score(
    query="thud", k=1, filter={"source": "https://example.com"}
)
for doc, score in results:
    print(f"* [SIM={score:3f}] {doc.page_content} [{doc.metadata}]")
```

```output
* [SIM=0.414035] foo [{'source': 'https://example.com', 'id': '1'}]
```

### 通过转换为检索器进行查询 (Query by turning into retriever)

您也可以将向量存储转换为检索器 (retriever)，以便在链中使用更方便。

```python
retriever = vector_store.as_retriever(search_type="mmr", search_kwargs={"k": 1})
retriever.invoke("thud")
```

```output
[Document(metadata={'source': 'https://example.com', 'id': '1'}, page_content='foo')]
```

## 用于检索增强生成 (RAG) 的使用方法

有关如何将此向量存储用于检索增强生成 (RAG) 的指南，请参阅以下部分：

* [教程 (Tutorials](/oss/python/langchain/rag)
* [操作指南：使用 RAG 进行问答 (How-to: Question and answer with RAG)](https://python.langchain.com/docs/how_to/#qa-with-rag)
* [检索概念文档 (Retrieval conceptual docs)](https://python.langchain.com/docs/concepts/retrieval)

---

## API 参考 (API reference)

有关所有 DatabricksVectorSearch 功能和配置的详细文档，请访问 API 参考：[api-docs.databricks.com/python/databricks-ai-bridge/latest/databricks_langchain.html#databricks_langchain.DatabricksVectorSearch](https://api-docs.databricks.com/python/databricks-ai-bridge/latest/databricks_langchain.html#databricks_langchain.DatabricksVectorSearch)

---

<Callout icon="pen-to-square" iconType="regular">
    [Edit the source of this page on GitHub.](https://github.com/langchain-ai/docs/edit/main/src/oss\python\integrations\vectorstores\databricks_vector_search.mdx)
</Callout>
<Tip icon="terminal" iconType="regular">
    [Connect these docs programmatically](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Tip>
