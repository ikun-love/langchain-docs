---
title: SurrealDB向量存储
---

> [SurrealDB](https://surrealdb.com) 是一个专为 AI 系统构建的统一多模型数据库。它将结构化和非结构化数据（包括向量搜索、图遍历、关系查询、全文搜索、文档存储和时间序列数据）整合到一个符合 ACID 规范的引擎中，可从 3 MB 的边缘二进制文件扩展到云端的 PB 级集群。通过消除对多个专用存储的需求，SurrealDB 简化了架构、降低了延迟，并确保了 AI 工作负载的一致性。
>
> **为什么 SurrealDB 对生成式 AI 系统至关重要**
>
> - **存储和内存一体化引擎：** 在一个系统中结合持久化存储和快速、对 Agent 友好的内存，提供 Agent 所需的所有数据，无需同步多个系统。
> - **面向 Agent 的单跳内存：** 在单个查询中执行向量搜索、图遍历、语义连接和事务性写入，使 LLM Agent 能够快速、一致地访问内存，而无需将关系数据库、图数据库和向量数据库拼接在一起。
> - **原地推理和实时更新：** SurrealDB 允许 Agent 在数据旁边运行推理并接收毫秒级的最新更新，这对于实时推理和协作至关重要。
> - **版本化、持久化的上下文：** SurrealDB 支持时间旅行查询和版本化记录，使 Agent 能够审计或“回放”过去的 trạng（状态），以实现一致、可解释的推理。
> - **即插即用的 Agent 内存：** 将 AI 内存作为原生概念暴露出来，便于将 SurrealDB 用作 AI 框架的即插式后端。

本笔记本介绍如何开始使用 SurrealDB 向量存储。

## 设置 (Setup)

您可以本地运行 SurrealDB，或者从 [免费的 SurrealDB 云账户](https://surrealdb.com/docs/cloud/getting-started) 开始。

本地部署有两种选择：

1. [安装 SurrealDB](https://surrealdb.com/docs/surrealdb/installation)，然后 [运行 SurrealDB](https://surrealdb.com/docs/surrealdb/installation/running)。使用以下命令在内存中运行：

    ```bash
    surreal start -u root -p root
    ```

2. [使用 Docker 运行](https://surrealdb.com/docs/surrealdb/installation/running/docker)。

    ```bash
    docker run --rm --pull always -p 8000:8000 surrealdb/surrealdb:latest start
    ```

## 安装依赖项 (Install dependencies)

安装 `langchain-surrealdb` 和 `surrealdb` Python 包。

```shell
# -- 使用 pip
pip install -U langchain-surrealdb surrealdb
# -- 使用 poetry
poetry add langchain-surrealdb surrealdb
# -- 使用 uv
uv add langchain-surrealdb surrealdb
```

要运行此笔记本，我们只需要安装此示例所需的额外依赖项：

```python
!poetry add --quiet --group docs langchain-ollama langchain-surrealdb
```

## 初始化 (Initialization)

```python
from langchain_ollama import OllamaEmbeddings
from langchain_surrealdb.vectorstores import SurrealDBVectorStore
from surrealdb import Surreal

conn = Surreal("ws://localhost:8000/rpc")
conn.signin({"username": "root", "password": "root"})
conn.use("langchain", "demo")
vector_store = SurrealDBVectorStore(OllamaEmbeddings(model="llama3.2"), conn)
```

## 管理向量存储 (Manage vector store)

### 向向量存储添加条目 (Add items to vector store)

```python
from langchain_core.documents import Document

_url = "https://surrealdb.com"
d1 = Document(page_content="foo", metadata={"source": _url})
d2 = Document(page_content="SurrealDB", metadata={"source": _url})
d3 = Document(page_content="bar", metadata={"source": _url})
d4 = Document(page_content="this is surreal", metadata={"source": _url})

vector_store.add_documents(documents=[d1, d2, d3, d4], ids=["1", "2", "3", "4"])
```

```output
['1', '2', '3', '4']
```

### 更新向量存储中的条目 (Update items in vector store)

```python
updated_document = Document(
    page_content="zar", metadata={"source": "https://example.com"}
)

vector_store.add_documents(documents=[updated_document], ids=["3"])
```

```output
['3']
```

### 删除向量存储中的条目 (Delete items from vector store)

```python
vector_store.delete(ids=["3"])
```

## 查询向量存储 (Query vector store)

一旦创建了向量存储并将相关文档添加进去，在运行链或 Agent 过程中，您很可能希望对其进行查询。

### 直接查询 (Query directly)

可以按如下方式执行简单的相似性搜索：

```python
results = vector_store.similarity_search(
    query="surreal", k=1, custom_filter={"source": "https://surrealdb.com"}
)
for doc in results:
    print(f"{doc.page_content} [{doc.metadata}]")
```

```output
this is surreal [{'source': 'https://surrealdb.com'}]
```

如果要执行相似性搜索并接收相应的分数，可以运行：

```python
results = vector_store.similarity_search_with_score(
    query="thud", k=1, custom_filter={"source": "https://surrealdb.com"}
)
for doc, score in results:
    print(f"[similarity={score:.0%}] {doc.page_content}")
```

```output
[similarity=57%] this is surreal
```

### 通过转换为检索器进行查询 (Query by turning into retriever)

您也可以将向量存储转换为检索器（retriever），以便在链中更轻松地使用。

```python
retriever = vector_store.as_retriever(
    search_type="mmr", search_kwargs={"k": 1, "lambda_mult": 0.5}
)
retriever.invoke("surreal")
```

```output
[Document(id='4', metadata={'source': 'https://surrealdb.com'}, page_content='this is surreal')]
```

## 用于检索增强生成的使用场景 (Usage for retrieval-augmented generation)

有关如何将此向量存储用于检索增强生成（RAG）的指南，请参阅以下部分：

- [How-to: Question and answer with RAG](https://python.langchain.com/docs/how_to/#qa-with-rag)
- [Retrieval conceptual docs](https://python.langchain.com/docs/concepts/retrieval/)

---

## API 参考 (API reference)

有关 `SurrealDBVectorStore` 所有功能和配置的详细文档，请查阅 API 参考：[python.langchain.com/api_reference/surrealdb/index.html](https://python.langchain.com/api_reference/surrealdb/index.html)

## 后续步骤 (Next steps)

- 查看 [基础示例 (basic example)](https://github.com/surrealdb/langchain-surrealdb/tree/main/examples/basic)。使用 Dockerfile 试一试！
- 查看 [图示例 (graph example)](https://github.com/surrealdb/langchain-surrealdb/tree/main/examples/graph)
- 尝试 [Jupyter 笔记本 (jupyter notebook)](https://github.com/langchain-ai/langchain/blob/v0.3/docs/docs/integrations/vectorstores/surrealdb.ipynb)
- [Awesome SurrealDB](https://github.com/surrealdb/awesome-surreal)，一个精选的 SurrealDB 资源、工具、实用程序和应用的列表

---

<Callout icon="pen-to-square" iconType="regular">
    [Edit the source of this page on GitHub.](https://github.com/langchain-ai/docs/edit/main/src/oss\python\integrations\vectorstores\surrealdb.mdx)
</Callout>
<Tip icon="terminal" iconType="regular">
    [Connect these docs programmatically](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Tip>
