---
title: Atlas
---

> [Atlas](https://docs.nomic.ai/index.html) 是 Nomic 公司推出的一个平台，专为处理小型到互联网规模的非结构化数据集而设计。它使用户能够在浏览器中对海量数据集进行可视化、搜索和共享。

您需要使用 `pip install -qU langchain-community` 来安装 `langchain-community` 库，才能使用此集成。

本教程展示了如何使用与 `AtlasDB` 向量存储相关的常用功能。

```python
pip install -qU  spacy
```

```python
!python3 -m spacy download en_core_web_sm
```

```python
pip install -qU  nomic
```

### 加载包 (Load Packages)

```python
import time

from langchain_community.document_loaders import TextLoader
from langchain_community.vectorstores import AtlasDB
from langchain_text_splitters import SpacyTextSplitter
```

```python
ATLAS_TEST_API_KEY = "7xDPkYXSYDc1_ErdTPIcoAR9RNd8YDlkS3nVNXcVoIMZ6"
```

### 准备数据 (Prepare the Data)

```python
loader = TextLoader("../../how_to/state_of_the_union.txt")
documents = loader.load()
text_splitter = SpacyTextSplitter(separator="|")
texts = []
for doc in text_splitter.split_documents(documents):
    # 按照 "|" 符号分割文档内容
    texts.extend(doc.page_content.split("|"))

texts = [e.strip() for e in texts]
```

### 使用 Nomic 的 Atlas 映射数据 (Map the Data using Nomic's Atlas)

```python
db = AtlasDB.from_texts(
    texts=texts,
    name="test_index_" + str(time.time()),  # 为您的向量存储设置一个唯一的名称
    description="test_index",  # 为您的向量存储提供一个描述
    api_key=ATLAS_TEST_API_KEY,
    index_kwargs={"build_topic_model": True}, # 启用主题模型构建
)
```

```python
db.project.wait_for_project_lock()
```

```python
db.project
```

以下是此代码运行结果的地图。此地图显示了“国情咨文”的所有文本内容。
[atlas.nomic.ai/map/3e4de075-89ff-486a-845c-36c23f30bb67/d8ce2284-8edb-4050-8b9b-9bb543d7f647](https://atlas.nomic.ai/map/3e4de075-89ff-486a-845c-36c23f30bb67/d8ce2284-8edb-4050-8b9b-9bb543d7f647)

---

<Callout icon="pen-to-square" iconType="regular">
    [Edit the source of this page on GitHub.](https://github.com/langchain-ai/docs/edit/main/src/oss\python\integrations\vectorstores\atlas.mdx)
</Callout>
<Tip icon="terminal" iconType="regular">
    [Connect these docs programmatically](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Tip>
