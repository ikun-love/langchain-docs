---
title: OpenLM
---

[OpenLM](https://github.com/r2d4/openlm) 是一个零依赖项、兼容 OpenAI 接口的大型语言模型（LLM）提供商，它可以通过 HTTP 直接调用不同的推理端点（Inference Endpoints）。

它实现了 OpenAI 的 Completion 类，因此可以作为 OpenAI API 的即插即用（drop-in）替代品使用。此代码更改利用了 `BaseOpenAI` 基类，从而最大限度地减少了新增的代码量。

本示例将介绍如何使用 LangChain 同时与 OpenAI 和 HuggingFace 进行交互。您需要同时拥有这两个平台的 API 密钥。

### 设置 (Setup)

安装依赖项并设置 API 密钥。

```python
# 如果尚未安装 openlm 和 openai，取消注释以下行进行安装

pip install -qU  openlm
pip install -qU  langchain-openai
```

```python
import os
from getpass import getpass

# 检查 OPENAI_API_KEY 环境变量是否已设置
if "OPENAI_API_KEY" not in os.environ:
    print("请输入您的 OpenAI API 密钥:")
    os.environ["OPENAI_API_KEY"] = getpass()

# 检查 HF_API_TOKEN 环境变量是否已设置
if "HF_API_TOKEN" not in os.environ:
    print("请输入您的 HuggingFace Hub API 密钥:")
    os.environ["HF_API_TOKEN"] = getpass()
```

### 将 LangChain 与 OpenLM 结合使用

在这里，我们将在一个 `LLMChain` 中调用两个模型：来自 OpenAI 的 `text-davinci-003` 和 HuggingFace 上的 `gpt2` 模型。

```python
from langchain.chains import LLMChain
from langchain_community.llms import OpenLM
from langchain_core.prompts import PromptTemplate
```

```python
question = "What is the capital of France?"
template = """Question: {question}

Answer: Let's think step by step."""

prompt = PromptTemplate.from_template(template)

for model in ["text-davinci-003", "huggingface.co/gpt2"]:
    llm = OpenLM(model=model)
    llm_chain = LLMChain(prompt=prompt, llm=llm)
    result = llm_chain.run(question)
    print(
        """Model: {}
Result: {}""".format(model, result)
    )
```

```output
Model: text-davinci-003
Result:  France is a country in Europe. The capital of France is Paris.
Model: huggingface.co/gpt2
Result: Question: What is the capital of France?

Answer: Let's think step by step. I am not going to lie, this is a complicated issue, and I don't see any solutions to all this, but it is still far more
```

---

<Callout icon="pen-to-square" iconType="regular">
    [Edit the source of this page on GitHub.](https://github.com/langchain-ai/docs/edit/main/src/oss\python\integrations\llms\openlm.mdx)
</Callout>
<Tip icon="terminal" iconType="regular">
    [Connect these docs programmatically](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Tip>
