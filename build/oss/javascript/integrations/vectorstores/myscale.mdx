---
title: MyScale
---

<Tip>
**兼容性**

仅在 Node.js 环境中可用。
</Tip>

[MyScale](https://myscale.com/) 是一个新兴的 AI 数据库，它将向量搜索和 SQL 分析能力相结合，提供了一种托管式、高效且响应迅速的用户体验。

## 设置 (Setup)

1. 通过 [MyScale 的 Web 控制台](https://console.myscale.com/) 启动一个集群。有关详细信息，请参阅 [MyScale 的官方文档](https://docs.myscale.com/en/quickstart/)。
2. 启动集群后，在集群的 `Actions` 菜单中查看您的 `Connection Details`（连接详情）。您需要主机 (host)、端口 (port)、用户名 (username) 和密码 (password)。
3. 在您的工作区安装所需的 Node.js 对等依赖项（peer dependency）。

<Tip>
有关安装 LangChain 包的一般说明，请参阅[本节](/oss/javascript/langchain/install)。
</Tip>

```bash npm
npm install -S @langchain/openai @clickhouse/client @langchain/community @langchain/core
```

## 索引和查询文档

```typescript
import { MyScaleStore } from "@langchain/community/vectorstores/myscale";
import { OpenAIEmbeddings } from "@langchain/openai";

const vectorStore = await MyScaleStore.fromTexts(
  ["Hello world", "Bye bye", "hello nice world"],
  [
    { id: 2, name: "2" },
    { id: 1, name: "1" },
    { id: 3, name: "3" },
  ],
  new OpenAIEmbeddings(),
  {
    host: process.env.MYSCALE_HOST || "localhost",
    port: process.env.MYSCALE_PORT || "8443",
    username: process.env.MYSCALE_USERNAME || "username",
    password: process.env.MYSCALE_PASSWORD || "password",
    database: "default", // 默认为 "default"
    table: "your_table", // 默认为 "vector_table"
  }
);

const results = await vectorStore.similaritySearch("hello world", 1);
console.log(results);

const filteredResults = await vectorStore.similaritySearch("hello world", 1, {
  whereStr: "metadata.name = '1'",
});
console.log(filteredResults);
```

## 从现有集合中查询文档

```typescript
import { MyScaleStore } from "@langchain/community/vectorstores/myscale";
import { OpenAIEmbeddings } from "@langchain/openai";

const vectorStore = await MyScaleStore.fromExistingIndex(
  new OpenAIEmbeddings(),
  {
    host: process.env.MYSCALE_HOST || "localhost",
    port: process.env.MYSCALE_PORT || "8443",
    username: process.env.MYSCALE_USERNAME || "username",
    password: process.env.MYSCALE_PASSWORD || "password",
    database: "default", // 默认为 "default"
    table: "your_table", // 默认为 "vector_table"
  }
);

const results = await vectorStore.similaritySearch("hello world", 1);
console.log(results);

const filteredResults = await vectorStore.similaritySearch("hello world", 1, {
  whereStr: "metadata.name = '1'",
});
console.log(filteredResults);
```

## 相关内容

- 向量存储 [概念指南](/oss/javascript/integrations/vectorstores)
- 向量存储 [操作指南](/oss/javascript/integrations/vectorstores)

---

<Callout icon="pen-to-square" iconType="regular">
    [Edit the source of this page on GitHub.](https://github.com/langchain-ai/docs/edit/main/src/oss\javascript\integrations\vectorstores\myscale.mdx)
</Callout>
<Tip icon="terminal" iconType="regular">
    [Connect these docs programmatically](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Tip>
