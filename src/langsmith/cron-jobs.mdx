---
title: 使用 Cron 作业
sidebarTitle: 使用 Cron 作业
---

在许多情况下，按计划运行助手都是非常实用的。

例如，假设您正在构建一个助手，该助手每天运行并发送当天的每日新闻摘要邮件。您可以使用 cron 作业 (cron job) 约定每天下午 8:00 运行该助手。

LangSmith 部署支持 cron 作业，这些作业可以按照用户定义的计划运行。用户指定一个计划 (schedule)、一个助手 (assistant) 以及一些输入 (input)。之后，在指定的计划时间，服务器将执行以下操作：

* 使用指定的助手创建一个新的线程 (thread)
* 将指定的输入发送到该线程

请注意，这每次都会向线程发送**相同的**输入。

LangSmith 部署 API 提供了几个用于创建和管理 cron 作业的端点。有关更多详细信息，请参阅 [API 参考文档](https://langchain-ai.github.io/langgraph/cloud/reference/api/api_ref/)。

有时您不希望基于用户交互来运行图 (graph)，而是希望安排图在特定时间运行——例如，如果您希望图组合并发送团队的每周待办事项邮件。LangSmith 部署允许您通过使用 `Crons` 客户端来实现这一点，而无需编写自己的脚本。要安排一个图作业，您需要提供一个 [cron 表达式](https://crontab.cronhub.io/)，以告知客户端何时运行该图。`Cron` 作业在后台运行，不会干扰图的正常调用。

## 设置

首先，让我们设置 SDK 客户端、助手和线程：

<Tabs>
    <Tab title="Python">
    ```python
    from langgraph_sdk import get_client

    client = get_client(url=<DEPLOYMENT_URL>)
    # 使用部署名称为 "agent" 的图
    assistant_id = "agent"
    # 创建线程
    thread = await client.threads.create()
    print(thread)
    ```
    </Tab>
    <Tab title="Javascript">
    ```js
    import { Client } from "@langchain/langgraph-sdk";

    const client = new Client({ apiUrl: <DEPLOYMENT_URL> });
    // 使用部署名称为 "agent" 的图
    const assistantId = "agent";
    // 创建线程
    const thread = await client.threads.create();
    console.log(thread);
    ```
    </Tab>
    <Tab title="CURL">
    ```bash
    curl --request POST \
        --url <DEPLOYMENT_URL>/assistants/search \
        --header 'Content-Type: application/json' \
        --data '{
            "limit": 10,
            "offset": 0
        }' | jq -c 'map(select(.config == null or .config == {})) | .[0].graph_id' && \
    curl --request POST \
        --url <DEPLOYMENT_URL>/threads \
        --header 'Content-Type: application/json' \
        --data '{}'
    ```
    </Tab>
</Tabs>

输出：

```
{
'thread_id': '9dde5490-2b67-47c8-aa14-4bfec88af217',
'created_at': '2024-08-30T23:07:38.242730+00:00',
'updated_at': '2024-08-30T23:07:38.242730+00:00',
'metadata': {},
'status': 'idle',
'config': {},
'values': None
}
```

## 线程上的 Cron 作业

要创建与特定线程关联的 cron 作业，您可以按如下方式编写：

<Tabs>
    <Tab title="Python">
    ```python
    # 这将安排一个作业每天在 15:27 (下午 3:27) 运行
    cron_job = await client.crons.create_for_thread(
        thread["thread_id"],
        assistant_id,
        schedule="27 15 * * *",
        input={"messages": [{"role": "user", "content": "What time is it?"}]},
    )
    ```
    </Tab>
    <Tab title="Javascript">
    ```js
    // 这将安排一个作业每天在 15:27 (下午 3:27) 运行
    const cronJob = await client.crons.create_for_thread(
      thread["thread_id"],
      assistantId,
      {
        schedule: "27 15 * * *",
        input: { messages: [{ role: "user", content: "What time is it?" }] }
      }
    );
    ```
    </Tab>
    <Tab title="CURL">
    ```bash
    curl --request POST \
        --url <DEPLOYMENT_URL>/threads/<THREAD_ID>/runs/crons \
        --header 'Content-Type: application/json' \
        --data '{
            "assistant_id": <ASSISTANT_ID>,
        }'
    ```
    </Tab>
</Tabs>

请注意，**非常**重要的是删除不再需要的 `Cron` 作业。否则，您可能会产生不必要的 LLM API 费用！您可以使用以下代码删除一个 `Cron` 作业：

<Tabs>
    <Tab title="Python">
    ```python
    await client.crons.delete(cron_job["cron_id"])
    ```
    </Tab>
    <Tab title="Javascript">
    ```js
    await client.crons.delete(cronJob["cron_id"]);
    ```
    </Tab>
    <Tab title="CURL">
    ```bash
    curl --request DELETE \
        --url <DEPLOYMENT_URL>/runs/crons/<CRON_ID>
    ```
    </Tab>
</Tabs>

## 无状态 Cron 作业

您也可以通过使用以下代码创建无状态 cron 作业：

<Tabs>
    <Tab title="Python">
    ```python
    # 这将安排一个作业每天在 15:27 (下午 3:27) 运行
    cron_job_stateless = await client.crons.create(
        assistant_id,
        schedule="27 15 * * *",
        input={"messages": [{"role": "user", "content": "What time is it?"}]},
    )
    ```
    </Tab>
    <Tab title="Javascript">
    ```js
    // 这将安排一个作业每天在 15:27 (下午 3:27) 运行
    const cronJobStateless = await client.crons.create(
      assistantId,
      {
        schedule: "27 15 * * *",
        input: { messages: [{ role: "user", content: "What time is it?" }] }
      }
    );
    ```
    </Tab>
    <Tab title="CURL">
    ```bash
    curl --request POST \
        --url <DEPLOYMENT_URL>/runs/crons \
        --header 'Content-Type: application/json' \
        --data '{
            "assistant_id": <ASSISTANT_ID>,
        }'
    ```
    </Tab>
</Tabs>

再次提醒，请记得在完成工作后删除您的作业！

<Tabs>
    <Tab title="Python">
    ```python
    await client.crons.delete(cron_job_stateless["cron_id"])
    ```
    </Tab>
    <Tab title="Javascript">
    ```js
    await client.crons.delete(cronJobStateless["cron_id"]);
    ```
    </Tab>
    <Tab title="CURL">
    ```bash
    curl --request DELETE \
        --url <DEPLOYMENT_URL>/runs/crons/<CRON_ID>
    ```
    </Tab>
</Tabs>