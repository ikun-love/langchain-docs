---
title: Agent Server 更新日志
sidebarTitle: Agent Server 更新日志
---

[Agent Server](/langsmith/agent-server) 是一个 API 平台，用于创建和管理基于 Agent 的应用程序。它内置了持久化存储、任务队列，并支持大规模部署、配置和运行助手（Agentic 工作流）。本更新日志记录了 Agent Server 发布的**所有值得注意的更新、新特性和修复**。

<a id="2025-12-10"></a>
## v0.5.39
- 完成了运行（runs）和定时任务（crons）的自定义加密实现，并简化了加密流程。
- 在 `values` 和 `updates` 流模式中引入了对子图事件（subgraph events）的流式传输支持。

<a id="2025-12-10"></a>
## v0.5.38
- 为线程（threads）实现了完整的自定义加密，确保所有线程数据得到适当保护和加密。
- 确保 Redis 尝试标志（attempt flags）被一致地过期，以防止陈旧数据。
- 添加了核心身份验证以及对 OR/AND 过滤器的支持，增强了安全性和灵活性。

<a id="2025-12-09"></a>
## v0.5.37
- 在助手（assistants）计数 API 中添加了 `name` 参数，以提高搜索灵活性。

<a id="2025-12-09"></a>
## v0.5.36
- 引入了可配置的 Webhook 支持，允许用户自定义提交的 Webhook 和请求头（headers）。
- 在根目录下添加了 `/ok` 端点，便于进行健康检查和简化配置。

<a id="2025-12-08"></a>
## v0.5.34
- 引入了自定义加密中间件（middleware），允许用户定义自己的加密方法以增强数据保护。

<a id="2025-12-08"></a>
## v0.5.33
- 将 Uvicorn 的保持活动（keep-alive）超时时间设置为 75 秒，以防止偶尔出现的 502 错误并改善连接处理。

<a id="2025-12-06"></a>
## v0.2.32
- 引入了 OpenTelemetry 遥测代理（telemetry agent），并支持与 New Relic 的集成。

<a id="2025-12-05"></a>
## v0.5.31
- 启用了 Py-Spy 分析功能，用于改进部署性能的分析，但在覆盖范围上存在一些限制。

<a id="2025-12-05"></a>
## v0.5.30
- 始终配置回环（loopback）传输客户端，以增强可靠性。
- 确保在 JS 的远程非流式方法中传递了身份验证请求头（authentication headers）。

<a id="2025-12-04"></a>
## v0.5.28
- 引入了基于 Rust 的更快速的 uuid7 实现以提高性能，现已在 langsmith 和 langchain-core 中使用。
- 在 PostgreSQL 身份验证过滤器中添加了对 `$or` 和 `$and` 的支持，以便在身份验证检查中实现复杂逻辑。
- 限制了 psycopg 和 psycopg-pool 的版本，以防止在启动时无限期等待。

<a id="2025-11-26"></a>
## v0.5.27
- 确保带过滤器的 `runs.list` 调用只返回运行（run）字段，防止错误的状态数据被包含在内。
- (JS) 将 `uuid` 从 v10.0.0 升级到 v13.0.0，并将 `exit-hook` 从 v4.0.0 升级到 v5.0.1。

<a id="2025-11-24"></a>
## v0.5.26
- 解决了在 JavaScript 环境中使用 `store.put` 时，如果没有使用 `AsyncBatchedStore` 存在的问题。

<a id="2025-11-22"></a>
## v0.5.25
- 引入了通过新端点按 `name` 搜索助手（assistants）的功能。
- 在 JavaScript 中将 `store_get` 的返回类型转换为元组（tuple），以确保类型一致性。

<a id="2025-11-21"></a>
## v0.5.24
- 为 Datadog 添加了执行器指标（executor metrics），并增强了核心流 API 指标，以便更好地跟踪性能。
- 禁用了 Redis Go 的维护通知，以防止在 Redis 版本低于 8 时因不受支持的命令而导致启动错误。

<a id="2025-11-20"></a>
## v0.5.20
- 解决了在处理大消息时执行器服务中发生的错误。

<a id="2025-11-19"></a>
## v0.5.19
- 将内置的 langchain-core 升级到 v1.0.7，以解决提示词格式化（prompt formatting）漏洞。

<a id="2025-11-19"></a>
## v0.5.18
- 引入了带有 `on_run_completed: {keep,delete}` 的持久化定时任务线程，以增强定时任务管理和检索选项。

<a id="2025-11-19"></a>
## v0.5.17
- 增强了任务处理能力，以支持多次中断（interrupts），与开源功能保持一致。

<a id="2025-11-18"></a>
## v0.5.15
- 为 `Resume` 和 `Goto` 命令添加了自定义 JSON 反序列化（unmarshalling），以修复类字典（map-style）的 null 恢复解释问题。

<a id="2025-11-14"></a>
## v0.5.14
- 确保 `pg make start` 命令在启用 core-api 时能正确运行。

<a id="2025-11-13"></a>
## v0.5.13
- 支持使用 `include` 和 `exclude`（作为 `includes` 和 `excludes` 的复数形式键），因为文档先前错误地声称仅支持后者。现在服务器接受两者。

<a id="2025-11-10"></a>
## v0.5.11
- 确保在流式传输线程时一致地应用身份验证处理程序（auth handlers），以遵循最新的安全实践。
- 将 `undici` 依赖项从 v6.21.3 升级到 v7.16.0，引入了各种性能改进和错误修复。
- 将 `p-queue` 从 v8.0.1 升级到 v9.0.0，引入了新功能和破坏性变更，包括移除了 `throwOnTimeout` 选项。

<a id="2025-11-10"></a>
## v0.5.10
- 在队列的 `/ok` 处理程序中实现了健康检查调用，以改善 Kubernetes 的存活（liveness）和准备就绪（readiness）探测兼容性。

<a id="2025-11-09"></a>
## v0.5.9
- 解决了在 SIGINT 中断期间，`elapsed` 变量出现“未绑定局部变量错误”（unbound local error）的问题。
- 将“中断”（interrupted）状态映射到 A2A 的“需要输入”（input-required）状态，以更好地对齐任务状态。

<a id="2025-11-07"></a>
## v0.5.8
- 确保在启动 `langgraph-ui` 时将环境变量作为字典传递，以兼容 `uvloop`。
- 在 Go 中实现了运行（runs）的 CRUD 操作，简化了 JSON 合并并提高了事务的可读性，以 PostgreSQL 为参考。

<a id="2025-11-07"></a>
## v0.5.7
- 用带重试（retry）的客户端替换了无重试的 Redis 客户端，以更有效地处理连接错误，并降低了相应的日志记录级别。

<a id="2025-11-06"></a>
## v0.5.6
- 添加了挂起时间指标（pending time metrics），以便更好地了解任务等待时间。
- 使用 `ChannelValue` 替换了 `pb.Value` 以简化代码结构。

<a id="2025-11-05"></a>
## v0.5.5
- 使得 Redis 的 `health_check_interval` 更加频繁和可配置，以便更好地处理空闲连接。

<a id="2025-11-05"></a>
## v0.5.4
- 实现了带 `OPT_REPLACE_SURROGATES` 的 `ormsgpack`，并进行了更新以兼容最新的 FastAPI 版本（该版本影响自定义身份验证依赖项）。

<a id="2025-11-03"></a>
## v0.5.2
- 在启动过程中为 PostgreSQL 连接添加了重试逻辑，以增强部署的可靠性，并改进了错误日志记录以方便调试。

<a id="2025-11-03"></a>
## v0.5.1
- 解决了使用 LangChain.js 的 `createAgent` 功能时持久化（persistence）无法正常工作的问题。
- 通过改进数据库连接池和重新使用 gRPC 客户端，优化了助手（assistants）的 CRUD 性能，减少了大负载数据（large payloads）的延迟。

<a id="2025-10-31"></a>
## v0.5.0

此小版本现在要求 `langgraph-checkpoint` 版本晚于 3.0，以防止 `langgraph-checkpoint` 库早期版本中存在的反序列化漏洞。
`langgraph-checkpoint` 库兼容 `langgraph` 的小版本 0.4、0.5、0.6 和 1.0。

此版本移除了对使用“json”类型保存的负载进行反序列化的默认支持，该类型从未是默认设置。
默认情况下，对象使用 msgpack 序列化。在某些不常见的情况下，负载曾使用旧的“json”模式进行序列化。如果这些负载包含自定义 Python 对象，除非提供 `serde` 配置，否则将不再能够反序列化它们：

```json
{
    "checkpointer": {
        "serde": {
            "allowed_json_modules": [
                ["my_agent", "my_file", "SomeType"],
            ]
        }
    }
}
```

<a id="2025-10-29"></a>
## v0.4.47
- 使用 `TypeAdapter` 验证并自动更正了环境配置类型。
- 添加了对 LangChain.js 和 LangGraph.js v1.x 的支持，确保兼容性。
- 将 hono 库从 v4.9.7 升级到 v4.10.3，解决了 CORS 中间件的安全问题并增强了 JWT 受众（audience）验证。
- 引入了模块化的基准测试框架（benchmark framework），添加了对助手和流的支持，并改进了现有的斜坡（ramp）基准测试方法。
- 为核心线程 CRUD 操作引入了 gRPC API，并更新了 Python 和 TypeScript 客户端。
- 将 `hono` 包从 v4.9.7 升级到 v4.10.2，包括 JWT 受众验证的安全改进。
- 将 `hono` 依赖项从 v4.9.7 升级到 v4.10.3，以修复安全问题并改进 CORS 中间件处理。
- 为线程引入了基本的 CRUD 操作，包括创建、获取、修补、删除、搜索、计数和复制，并支持 Go、gRPC 服务器以及 Python 和 TypeScript 客户端。

<a id="2025-10-21"></a>
## v0.4.46
- 添加了一个选项以启用从子图事件进行消息流式传输，使用户能够更好地控制事件通知。

<a id="2025-10-21"></a>
## v0.4.45
- 实现了对自定义路由（custom routes）的授权支持，通过 `enable_custom_route_auth` 标志控制。
- 默认关闭了跟踪（tracing），以提高性能并简化调试。

<a id="2025-10-18"></a>
## v0.4.44
- 为与许可证相关的键使用了 Redis 键前缀，以防止与现有设置冲突。

<a id="2025-10-16"></a>
## v0.4.43
- 实现了对 Redis 连接的健康检查，以防止连接空闲超时。

<a id="2025-10-15"></a>
## v0.4.40
- 通过解决竞争条件并添加测试以确保一致行为，防止了在可恢复运行和线程流中出现重复消息。
- 确保运行在收到 pubsub 订阅确认后才启动，以防止启动时消息丢失。
- 重新命名平台为 langgraph，以提高清晰度和品牌形象。
- 在使用后重置 PostgreSQL 连接以防止锁持有，并改进了事务问题的错误报告。

<a id="2025-10-10"></a>
## v0.4.39
- 将 `hono` 从 v4.7.6 升级到 v4.9.7，解决了与 `bodyLimit` 中间件相关的安全问题。
- 允许自定义基础身份验证 URL，以增强灵活性。
- 使用 'uv' 将 'ty' 依赖项固定到稳定版本，以防止意外的 linting 失败。

<a id="2025-10-08"></a>
## v0.4.38
- 使用 `LANGSMITH_CONTROL_PLANE_API_KEY` 替换了 `LANGSMITH_API_KEY`，以支持需要许可证验证的混合部署。
- 引入了自托管日志摄取（log ingestion）支持，可通过 `SELF_HOSTED_LOGS_ENABLED` 和 `SELF_HOSTED_LOGS_ENDPOINT` 环境变量进行配置。

<a id="2025-10-06"></a>
## v0.4.37
- 需要创建权限才能复制线程，以确保正确的授权。

<a id="2025-10-03"></a>
## v0.4.36
- 改进了错误处理，并在清除循环（sweep loop）中添加了延迟，以便在 Redis 停机或取消错误期间进行更平稳的操作。
- 更新了队列入口点，在启用 `FF_USE_CORE_API` 时启动核心 API gRPC 服务器。
- 在助手端点中引入了对无效配置的检查，以确保与其他端点的一致性。

<a id="2025-10-02"></a>
## v0.4.35
- 解决了核心 API 中的时区问题，确保时间数据检索准确。
- 引入了新的 `middleware_order` 设置，以在自定义中间件之前应用身份验证中间件，从而可以更精细地控制受保护的路由配置。
- 在创建 Redis 客户端出错时记录 Redis URL。
- 改进了 Go 引擎/运行时上下文传播，以确保执行流程一致。
- 从执行器入口点移除了不必要的 `assistants.put` 调用，以简化流程。

<a id="2025-10-01"></a>
## v0.4.34
- 阻止未经授权的用户更新线程 TTL 设置，以增强安全性。

<a id="2025-10-01"></a>
## v0.4.33
- 改进了对 Redis 锁的错误处理，通过记录 `LockNotOwnedError` 并将初始连接池迁移锁超时延长至 60 秒。
- 更新了 BaseMessage 模式以与最新的 langchain-core 版本对齐，并同步了构建依赖项以实现一致的本地开发。

<a id="2025-09-30"></a>
## v0.4.32
- 向 API 镜像添加了 GO 持久层，以通过 PostgreSQL 支持启用 GRPC 服务器操作并增强可配置性。
- 在发生超时时将状态设置为 error，以改进错误处理。

<a id="2025-09-30"></a>
## v0.4.30
- 添加了在使用 `stream_mode="events"` 时的上下文支持，并为此功能添加了新的测试。
- 添加了使用 `$LANGGRAPH_SERVER_PORT` 覆盖服务器端口的支持，并移除了不必要的 Dockerfile `ARG` 以简化配置。
- 对线程删除 CTE 中的所有表引用应用了授权过滤器，以增强安全性。
- 引入了自托管指标摄取（metrics ingestion）功能，当设置了相应的环境变量时，每分钟将指标发送到 OTLP 收集器。
- 确保 `set_latest` 函数正确地更新版本的名称和描述。

<a id="2025-09-26"></a>
## v0.4.29
- 确保在所有场景下正确清理 redis pubsub 连接。

<a id="2025-09-25"></a>
## v0.4.28
- 为队列指标服务器添加了 `format` 参数以增强自定义性。
- 修正了 CLI 中 `MOUNT_PREFIX` 环境变量的使用，以与文档保持一致并防止混淆。
- 添加了一个功能，用于在消息因没有订阅者而被丢弃时记录警告，可通过功能标志控制。
- 在 Node 镜像中添加了对 Bookworm 和 Bullseye 发行版的支持。
- 合并了执行器定义，将它们从 `langgraph-go` 存储库中移出，提高了可管理性，并针对服务器迁移更新了检查点设置方法。
- 确保为 a2a 发送了正确的响应头，以提高兼容性和通信。
- 整合了 PostgreSQL 检查点实现，向 `/core` 目录添加了 CI 测试，修复了 RemoteStore 测试错误，并通过事务增强了 Store 实现。
- 向队列服务器添加了 PostgreSQL 迁移，以防止在执行迁移之前添加图表（graphs）时出错。

<a id="2025-09-23"></a>
## v0.4.27
- 使用 `redis-py` 替换了 `coredis`，以在流量高峰期改进连接处理和可靠性。

<a id="2025-09-22-v0.4.24"></a>
## v0.4.24
- 添加了根据 A2A 规范返回完整消息历史记录的功能。
- 在 Dockerfiles 中添加了 `LANGGRAPH_SERVER_HOST` 环境变量，以支持双栈模式（dual stack mode）的自定义主机设置。

<a id="2025-09-22"></a>
## v0.4.23
- 对 Redis 流式传输使用更快的消息编解码器（codec）。

<a id="2025-09-19"></a>
## v0.4.22
- 将长流处理移植到运行流（run stream）、连接（join）和取消（cancel）端点，以改进流管理。

<a id="2025-09-18"></a>
## v0.4.21
- 添加了 A2A 流式传输功能，并通过 A2A SDK 增强了测试。
- 为图表、中间件和身份验证中的语言使用情况添加了 Prometheus 指标，以提供更深入的洞察。
- 修复了开源软件中与块（chunks）的消息转换相关的错误。
- 移除了对 pubsub 订阅的 `await`，以减少集群测试中的不稳定性，并在关闭套件中添加了重试以增强 API 稳定性。

<a id="2025-09-11"></a>
## v0.4.20
- 优化了 Pubsub 初始化，以防止开销并解决订阅时序问题，确保运行执行更平稳。

<a id="2025-09-11"></a>
## v0.4.19
- 通过解决 psycopg 版本 3.2.10 中引入的函数检查，移除了警告。

<a id="2025-09-11"></a>
## v0.4.17
- 过滤掉了带有挂载前缀（mount prefix）的日志，以减少日志输出的噪音。

<a id="2025-09-10"></a>
## v0.4.16
- 添加了对 a2a 中隐式线程创建的支持，以简化操作。
- 改进了分布式运行时流中的错误序列化和发出，从而可以进行更全面的测试。

<a id="2025-09-09"></a>
## v0.4.13
- 在健康端点中监控队列状态，以确保在 PostgreSQL 初始化失败时行为正确。
- 解决了扫描 ID 长度不相等的问题，以提高日志清晰度。
- 通过避免重新序列化 DR 负载（使用 msgpack 字节检查进行类 JSON 解析），增强了流式输出。

<a id="2025-09-04"></a>
## v0.4.12
- 即使在出现数据库连接问题时也确保返回指标。
- 优化了更新流，以防止不必要的数据传输。
- 在 `storage_postgres/langgraph-api-server` 中将 `hono` 从 v4.9.2 升级到 v4.9.6，以提高 URL 路径解析安全性。
- 为 LangSmith 访问调用添加了重试和内存缓存，以提高抵御单点故障的弹性。

<a id="2025-09-04"></a>
## v0.4.11
- 添加了对线程更新中 TTL（time-to-live，生存时间）的支持。

<a id="2025-09-04"></a>
## v0.4.10
- 在分布式运行时中，更新了 serde 逻辑，用于最终检查点到线程设置的转换。

<a id="2025-09-02"></a>
## v0.4.9
- 在搜索端点中添加了按 ID 过滤搜索结果的支持，以支持更精确的查询。
- 为助手端点包含了可配置的请求头，以增强请求定制性。
- 实现了简单的 A2A 端点，支持代理卡（agent card）检索、任务创建和任务管理。

<a id="2025-08-30"></a>
## v0.4.7
- 停止包含 x-api-key，以增强安全性。

<a id="2025-08-29"></a>
## v0.4.6
- 修复了连接流（joining streams）时的竞争条件，防止了重复的启动事件。

<a id="2025-08-29"></a>
## v0.4.5
- 确保检查点器在队列之前和之后正确启动和停止，以提高关闭和启动效率。
- 解决了队列取消时工作进程被过早取消的问题。
- 通过为 Redis 无法唤醒工作进程的情况添加回退机制，阻止了队列终止。

<a id="2025-08-28"></a>
## v0.4.4
- 为无状态运行将自定义身份验证的 `thread_id` 设置为 None，以防止冲突。
- 改进了 Go 运行时中的 Redis 信令，通过添加唤醒工作进程和 Redis 锁实现，并更新了扫描逻辑。

<a id="2025-08-27"></a>
## v0.4.3
- 为线程流（thread stream）添加了流模式（stream mode），以改进数据处理。
- 为运行（runs）添加了持久性参数（durability parameter），以改进数据持久性。

<a id="2025-08-27"></a>
## v0.4.2
- 确保在创建运行之前初始化 pubsub，以防止因消息丢失而导致的错误。

<a id="2025-08-25"></a>
## v0.4.0

小版本 0.4 带来了一些改进以及一些破坏性变更。

- 在线程流中正确地发出了尝试消息（attempt messages）。
- 通过在集群映射（cluster mapping）中仅使用线程 ID 进行哈希处理，减少了集群冲突，并通过 `stream_thread_cache` 提高了效率。
- 引入了线程的流端点，用于跟踪按顺序执行的运行中的所有输出。
- 使 PostgreSQL 中过滤查询构建器对格式错误的表达式更具鲁棒性，并改进了验证以防止潜在的安全风险。

此小版本还包含几项破坏性变更，以提高服务的可用性和安全性：

- 在此小版本中，我们停止了自动将请求头作为可配置值包含在运行中的做法。您可以通过在代理服务器配置中设置 **configurable_headers** 来选择加入特定的模式。
- 运行流事件 ID（用于可恢复流）现在的格式是 `ms-seq`，而不是以前的格式。我们保留对旧格式的向后兼容性，但建议新代码使用新格式。

<a id="2025-08-25"></a>
## v0.3.4
- 为 Redis/PG 连接池添加了自定义 Prometheus 指标，并将队列服务器切换到 Uvicorn/Starlette 以改进监控。
- 通过更正 shell 命令格式恢复了 Wolfi 镜像的构建，并添加了一个 Makefile 目标以使用 nginx 进行测试。

<a id="2025-08-22"></a>
## v0.3.3
- 为特定的 Redis 调用添加了超时，以防止工作进程保持活动状态。
- 更新了 Golang 运行时，并添加了 pytest 跳过（skips）以支持不受支持的功能，包括初始支持，用于将存储（store）传递给节点和消息流。
- 引入了反向代理设置，用于服务组合型（combined）的 Python 和 Node.js 图表，由 nginx 处理服务器路由，以支持 Node.js API 服务器的 Postgres/Redis 后端。

<a id="2025-08-21"></a>
## v0.3.1
- 为连接池添加了语句超时（statement timeout），以防止长时间运行的查询。

<a id="2025-08-21"></a>
## v0.3.0
- 设置了默认的 15 分钟语句超时，并实施了对长时间运行查询的监控，以确保系统效率。
- 停止将运行的可配置值传播到线程配置中，因为这可能在后续运行中（如果您指定了 `checkpoint_id` 时）导致问题。这是一个**轻微的破坏性变更**，因为线程值将不再自动反映最近运行的联合配置。然而，我们认为此行为更直观。
- 通过在 ops.py 中处理通道（channel）名称中的事件数据，增强了对旧版工作进程的兼容性。

<a id="2025-08-20"></a>
## v0.2.137
- 修复了未绑定局部变量错误（unbound local error），并改进了线程中断或错误的日志记录，以及类型更新。

<a id="2025-08-20"></a>
## v0.2.136
- 添加了增强的日志记录，以帮助调试元视图（metaview）问题。
- 将执行器和运行时升级到最新版本，以提高性能和稳定性。

<a id="2025-08-19"></a>
## v0.2.135
- 确保异步协程（coroutines）得到正确等待（awaited），以防止潜在的运行时错误。

<a id="2025-08-18"></a>
## v0.2.134
- 增强了搜索功能，通过允许用户选择特定列进行查询结果来提高性能。

<a id="2025-08-18"></a>
## v0.2.133
- 为定时任务（crons）、线程和助手添加了计数端点，以增强数据跟踪（#1132）。
- 改进了 SSH 功能，以提高可靠性和稳定性。
- 将 `@langchain/langgraph-api` 更新到 v0.0.59，以修复无效状态模式问题。

<a id="2025-08-15"></a>
## v0.2.132
- 添加了 Go 语言镜像，以增强项目兼容性和功能。
- 为 JS 工作进程打印内部 PID，以便通过 SIGUSR1 信号进行进程检查。
- 解决了尝试插入重复运行（duplicate runs）时发生的 `run_pkey` 错误。
- 添加了 `ty run` 命令，并切换到使用 uuid7 来生成运行 ID。
- 实现了初始 Golang 运行时，以扩展语言支持。

<a id="2025-08-14"></a>
## v0.2.131
- 在 JS 中添加了对带有描述的 `object agent spec` 的支持。

<a id="2025-08-13"></a>
## v0.2.130
- 添加了一个功能标志（FF_RICH_THREADS=false）以在创建运行时禁用线程更新，减少锁竞争并简化线程状态处理。
- 为 `aput` 和 `apwrite` 操作利用现有连接以提高性能。
- 改进了对解码问题的错误处理，以增强数据处理的可靠性。
- 从日志中排除了请求头，以提高安全性，同时保持运行时功能。
- 修复了一个阻止将槽位（slots）映射到单个节点的错误。
- 在 JS 部署中添加了调试日志以跟踪节点执行，以改进问题诊断。
- 将默认多任务处理策略更改为入队（enqueue），通过消除在插入新运行时需要获取正在进行的运行（inflight runs）的需要来提高吞吐量。
- 优化了 `Runs.next` 和 `Runs.sweep` 的数据库操作，以减少冗余查询并提高效率。
- 通过跳过不必要的正在进行的运行查询，提高了运行创建速度。

<a id="2025-08-11"></a>
## v0.2.129
- 停止将内部 LGP 字段传递给上下文，以防止破坏类型检查。
- 暴露了 content-location 请求头，以确保 API 中的正确可恢复性行为。

<a id="2025-08-08"></a>
## v0.2.128
- 确保助手中的 `configurable` 和 `context` 之间进行同步更新，防止了设置错误并支持更平稳的版本转换。

<a id="2025-08-08"></a>
## v0.2.127
- 从可恢复流中排除了未请求的流模式，以优化功能。

<a id="2025-08-08"></a>
## v0.2.126
- 使访问日志请求头可配置，以增强日志记录灵活性。
- 对 `Runs.stats` 函数进行了去抖（debounced）处理，以减少昂贵调用的频率并提高性能。
- 对清除器（sweepers）引入了去抖动（debouncing），以提高性能和效率（#1147）。
- 为了在规模扩展操作期间防止数据库垃圾信息，为 TTL 清除获取了锁。

<a id="2025-08-06"></a>
## v0.2.125
- 更新了跟踪上下文副本，使用了新格式，确保兼容性。

<a id="2025-08-06"></a>
## v0.2.123
- 为队列副本添加了入口点，以改进部署管理。

<a id="2025-08-06"></a>
## v0.2.122
- 在 `join` 中利用持久化的中断状态，以确保在完成用户中断状态后能正确处理。

<a id="2025-08-06"></a>
## v0.2.121
- 将事件整合到一个通道中，以防止竞争条件并优化启动性能。
- 确保在队列工作进程中调用自定义生命周期（lifespans）以进行正确设置，并添加了测试。

<a id="2025-08-04"></a>
## v0.2.120
- 恢复了运行的原始流式传输行为，确保根据 `stream_mode` 设置一致地包含中断事件。
- 优化了 `Runs.next` 查询，使平均执行时间从 ~14.43ms 降低到 ~2.42ms，提高了性能。
- 添加了对流模式 "tasks" 和 "checkpoints" 的支持，规范了 UI 命名空间，并升级了 `@langchain/langgraph-api` 以增强功能。

<a id="2025-07-31"></a>
## v0.2.117
- 在 `threads` 上添加了一个复合索引，用于基于所有者的身份验证的更快搜索，并将默认排序顺序更新为 `updated_at`，以提高查询性能。

<a id="2025-07-31"></a>
## v0.2.116
- 将历史检查点（history checkpoints）的默认数量从 10 个减少到 1 个，以优化性能。

<a id="2025-07-31"></a>
## v0.2.115
- 优化了缓存重用，以提高应用程序性能和效率。

<a id="2025-07-30"></a>
## v0.2.113
- 通过在响应头中更新 `X-Pagination-Total` 和 `X-Pagination-Next`，改进了线程搜索分页，以实现更好的导航。

<a id="2025-07-30"></a>
## v0.2.112
- 确保同步日志方法得到等待（awaited），并添加了 linter 以防止将来发生。
- 修复了 JavaScript 任务未正确填充到 JS 图中的问题。

<a id="2025-07-29"></a>
## v0.2.111
- 通过在连接打开时立即启动心跳（heartbeat），修复了 JS 图的流式传输失败问题。

<a id="2025-07-29"></a>
## v0.2.110
- 在连接（join）操作中添加了中断作为默认值，同时保留了流行为。

<a id="2025-07-28"></a>
## v0.2.109
- 修复了当未设置 `config_type` 时配置模式（config schema）丢失的问题，确保了更可靠的配置。

<a id="2025-07-28"></a>
## v0.2.108
- 为 LangGraph v0.6 兼容性做准备，支持新的上下文 API 和错误修复。

<a id="2025-07-27"></a>
## v0.2.107
- 实现了身份验证过程的缓存，以提高性能和效率。
- 通过合并计数和选择查询来优化数据库性能。

<a id="2025-07-27"></a>
## v0.2.106
- 使日志流可恢复，增强了可靠性，并改善了用户重新连接时的体验。

<a id="2025-07-27"></a>
## v0.2.105
- 添加了一个 `heapdump` 端点，用于将内存堆信息保存到文件中。

<a id="2025-07-25"></a>
## v0.2.103
- 使用正确的元数据端点来解决数据检索问题。

<a id="2025-07-24"></a>
## v0.2.102
- 在 `wait` 方法中捕获了中断事件，以保留 langgraph 0.5.0 中的先前行为。
- 在 JavaScript 环境中添加了对 SDK structlog 的支持，以增强日志记录功能。

<a id="2025-07-24"></a>
## v0.2.101
- 修正了自托管部署的元数据端点。

<a id="2025-07-22"></a>
## v0.2.99
- 通过添加内存缓存并更有效地处理 Redis 连接错误，改进了许可证检查。
- 重新加载助手以保留手动创建的助手，同时丢弃从配置文件中删除的助手。
- 撤销了更改，以确保生成 UI 的 UI 命名空间是有效的 JavaScript 属性名称。
- 确保生成的 UI 的 UI 命名空间是有效的 JavaScript 属性名称，以提高 API 合规性。
- 增强了错误处理，对无法处理的实体请求返回 422 状态码。

<a id="2025-07-19"></a>
## v0.2.98
- 向 langgraph 节点添加了上下文，以改进日志过滤和跟踪可见性。

<a id="2025-07-19"></a>
## v0.2.97
- 改进了主循环中 ckpt 摄取工作进程的互操作性，以防止任务调度问题。
- 推迟队列工作进程启动，直到迁移完成后才执行，以防止过早执行。
- 通过添加特定元数据和改进的响应代码，增强了线程状态的错误处理，以便在创建期间状态更新失败时有更好的清晰度。
- 在检索线程状态时暴露了中断 ID，以提高 API 透明度。

<a id="2025-07-17"></a>
## v0.2.96

* 为可配置的请求头模式添加了回退机制，以更有效地处理排除/包含设置。

<a id="2025-07-17"></a>
## v0.2.95

* 避免在未来（future）已经完成时设置它，以防止冗余操作。
* 通过切换到 `typing_extensions.TypedDict` 而不是 `typing.TypedDict`（针对 Python 版本低于 3.12），解决了 CI 中的兼容性错误。

<a id="2025-07-16"></a>
## v0.2.94

* 通过省略对 langgraph 版本 0.5 及更高版本的待发送（pending sends）来提高性能。
* 当设置了 `DD_API_KEY` 环境变量时，改进了服务器启动日志，以提供更清晰的警告。

<a id="2025-07-16"></a>
## v0.2.93

* 移除了运行元数据的 GIN 索引，以提高性能。

<a id="2025-07-16"></a>
## v0.2.92

* 启用了 blob 和检查点的复制功能，增强了数据管理灵活性。

<a id="2025-07-16"></a>
## v0.2.91

* 通过内联小值（null、numeric、str 等），减少了对 `checkpoint_blobs` 表的写入。这意味着我们不需要为尚未更新的通道存储额外的值。

<a id="2025-07-16"></a>
## v0.2.90

* 通过节点本地后台排队（node-local background queueing）来改进检查点写入。

<a id="2025-07-15"></a>
## v0.2.89

* 通过移除外键，将检查点写入与线程/运行状态解耦，并更新了日志记录器以防止与超时相关的失败。

<a id="2025-07-14"></a>
## v0.2.88

* 移除了 `run` 表中 `thread` 的外键约束，以简化数据库模式。

<a id="2025-07-14"></a>
## v0.2.87

* 为 Redis 工作进程信令添加了更详细的日志，以改进调试。

<a id="2025-07-11"></a>
## v0.2.86

* 在 `/mcp` 端点中遵循了工具描述（tool descriptions），以符合预期功能。

<a id="2025-07-10"></a>
## v0.2.85

* 为 `runs/wait` 添加了对 `on_disconnect` 字段的支持，并包含了断开连接日志以增强调试。

<a id="2025-07-09"></a>
## v0.2.84

* 移除了不必要的次状态更新，以简化线程处理，并将版本更新到 0.2.84。

<a id="2025-07-09"></a>
## v0.2.83

* 将可恢复流的默认生存时间（time-to-live）减少到 2 分钟。
* 增强了数据提交逻辑，根据许可证配置将数据同时发送到 Beacon 和 LangSmith 实例。
- 当配置了端点时，启用向自托管 LangSmith 实例提交数据。

<a id="2025-07-03"></a>
## v0.2.82

* 解决后台运行中的竞争条件，通过使用 `join` 实现锁，确保跨 CTE 的可靠执行。

<a id="2025-07-03"></a>
## v0.2.81

* 优化了运行流，减少了初始等待时间，以改善对较旧或不存在的运行的响应速度。

<a id="2025-07-03"></a>
## v0.2.80

* 修正了 `logger.ainfo()` API 调用中的参数传递，以解决 TypeError。

<a id="2025-07-02"></a>
## v0.2.79

* 修复了使用远程图（remote graph）进行检查点设置时的 JsonDecodeError，方法是更正 JSON 序列化以正确处理尾部斜杠。
- 引入了一个全局配置标志，以在所有路由中禁用 Webhook。

<a id="2025-07-02"></a>
## v0.2.78

* 为 Webhook 调用添加了超时重试，以提高可靠性。
- 添加了 HTTP 请求指标，包括请求计数和延迟直方图，以增强监控功能。

<a id="2025-07-02"></a>
## v0.2.77

* 添加了 HTTP 指标以提高性能监控。
- 更改了 Redis 缓存分隔符，以减少与子图消息名称的冲突，并更新了缓存行为。

<a id="2025-07-01"></a>
## v0.2.76

* 更新了 Redis 缓存分隔符，以防止与子图消息冲突。

<a id="2025-06-30"></a>
## v0.2.74

* 在隔离的循环中安排了 Webhook，以确保线程安全操作并防止在 `PYTHONASYNCIODEBUG=1` 时的错误。

<a id="2025-06-27"></a>
## v0.2.73

* 修复了无限帧循环问题，并由于 structlog 的意外行为而移除了 `dict_parser`。
- 在发生死锁时抛出 409 错误，以优雅地处理锁冲突。

<a id="2025-06-27"></a>
## v0.2.72

* 确保与未来 langgraph 版本的兼容性。
- 在取消期间实现 409 响应状态以处理死锁问题。

<a id="2025-06-26"></a>
## v0.2.71

* 改进了日志记录，以提供关于日志类型的更清晰和详细的信息。

<a id="2025-06-26"></a>
## v0.2.70

* 改进了错误处理，以更好地区分和记录由用户引起的超时错误和内部运行超时。

<a id="2025-06-26"></a>
## v0.2.69

* 为定时任务 API 添加了排序和分页，并更新了模式定义以提高准确性。

<a id="2025-06-26"></a>
## v0.2.66

* 修复了在使用 `on_not_exist="create"` 创建多个具有相同 `thread_id` 的运行时事产生的 404 错误。

<a id="2025-06-25"></a>
## v0.2.65

* 确保仅在需要时才返回 `assistant_versions` 中的字段。
* 确保内存中和 PostgreSQL 用户之间的数据类型一致，改进了内部身份验证处理。

<a id="2025-06-24"></a>
## v0.2.64

* 为版本条目添加了描述，以提高清晰度。

<a id="2025-06-23"></a>
## v0.2.62

* 改进了 JS Studio 中自定义身份验证的用户处理。
- 为指标端点添加了 Prometheus 格式的运行统计信息，以改善监控。
- 在指标端点中添加了运行统计信息（Prometheus 格式）。

<a id="2025-06-20"></a>
## v0.2.61

* 为 Redis 连接设置了最大空闲时间，以防止不必要的开放连接。

<a id="2025-06-20"></a>
## v0.2.60

* 增强了错误日志记录以包含字典操作的追踪详情（traceback details）。
- 为队列工作进程指标添加了 `/metrics` 端点，用于监控。

<a id="2025-06-18"></a>
## v0.2.57

* 从可重试异常中移除了 `CancelledError`，以允许本地中断，同时保持工作进程的可重试性。
- 引入了中间件，以在接收到 SIGINT 后，在完成进行中的请求后优雅地关闭服务器。
- 减少了检查点中存储的元数据，仅包含必要的信息。
- 改进了连接运行（join runs）中的错误处理，在存在错误详情时返回它们。

<a id="2025-06-17"></a>
## v0.2.56

* 通过为 SIGTERM 信号添加处理程序，提高了应用程序稳定性。

<a id="2025-06-17"></a>
## v0.2.55

* 改进了队列入口点中的取消处理。
- 改进了队列入口点中的取消处理。

<a id="2025-06-16"></a>
## v0.2.54

* 增强了许可证验证期间 LuaLock 超时的错误消息。
- 通过要求显式 `::text` 转换，修复了自定义身份验证中的 $contains 过滤器，并相应地更新了测试。
- 确保项目和租户 ID 格式化为 UUID，以保持一致性。

<a id="2025-06-13"></a>
## v0.2.53

* 解决了一个时序问题，以确保队列仅在图表注册后才启动。
- 通过在单个查询中设置线程和运行状态，提高了性能，并增强了检查点写入期间的错误处理。
- 将默认后台宽限期（background grace period）减少到 3 分钟。

<a id="2025-06-12"></a>
## v0.2.52

* 现在在省略图表时记录预期的图表，以提高可追溯性。
- 实现了可恢复流的时间到期（TTL）功能。
- 通过添加唯一索引和优化行锁定，提高了查询效率和一致性。

<a id="2025-06-12"></a>
## v0.2.51

* 通过将任务标记为可重试，处理了 `CancelledError`，改进了工作进程中的错误管理。
- 将 LG API 版本和请求 ID 添加到元数据和日志中，以实现更好的跟踪。
- 将 LG API 版本和请求 ID 添加到元数据和日志中，以提高可追溯性。
- 通过并发创建索引，提高了数据库性能。
- 确保 postgres 写入在设置 Redis 运行标记（running marker）之后才提交，以防止竞争条件。
- 通过添加唯一索引（在 `thread_id/running` 上）、优化行锁定以及确保确定性的运行选择，提高了查询效率和可靠性。
- 通过确保仅在设置 Redis 运行标记后才发生 Postgres 更新，解决了竞争条件。

<a id="2025-06-07"></a>
## v0.2.46

- 在 Threads 状态的 `update()` 和 `bulk()` 命令中，为每次操作引入了新连接，同时保持了事务特性。

<a id="2025-06-05"></a>
## v0.2.45

- 通过在流式传输功能中纳入跟踪上下文，增强了流式传输功能。
- 从 Crons.search 函数中移除了不必要的查询。
- 解决了调度多个定时任务的下一个运行时连接重用问题。
- 从 Crons.search 函数中移除了不必要的查询以提高效率。
- 通过改进连接重用，解决了调度下一个定时任务运行的问题。

<a id="2025-06-04"></a>
## v0.2.44

- 增强了工作进程逻辑，当达到 Redis 消息限制时，在管道（pipeline）继续之前退出。
- 引入了 Redis 消息大小的上限，并根据性能改进选择了跳过大于 128 MB 的消息的选项。
- 确保管道始终正确关闭，以防止资源泄漏。

<a id="2025-06-04"></a>
## v0.2.43

- 通过在元数据调用中省略日志并确保值流（value streaming）符合输出模式，提高了性能。
- 确保使用后正确关闭连接。
- 使输出格式严格遵循指定的模式。
- 停止在元数据请求中发送内部日志，以提高隐私性。

<a id="2025-06-04"></a>
## v0.2.42

- 添加了时间戳以跟踪请求运行的开始和结束。
- 将示踪器（tracer）信息添加到配置设置中。
- 添加了对带有跟踪上下文的流式传输支持。

<a id="2025-06-03"></a>
## v0.2.41

- 添加了锁定机制，以防止管道执行中的错误。