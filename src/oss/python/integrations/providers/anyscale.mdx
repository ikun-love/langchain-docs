---
title: Anyscale
---

> [Anyscale](https://www.anyscale.com) 是一个通过生产级 API 来运行、微调和扩展大型语言模型（LLM）的平台。
> [Anyscale Endpoints](https://docs.anyscale.com/endpoints/overview) 以高性价比的方式提供许多开源模型的服务。

`Anyscale` 还提供了一个[示例](https://docs.anyscale.com/endpoints/model-serving/examples/langchain-integration)，说明如何设置 `LangChain` 并与 `Anyscale` 结合使用，以构建高级聊天代理（Chat Agents）。

## 安装与设置 (Installation and Setup)

- 获取 Anyscale Service URL、路由（route）和 API 密钥，并将它们设置为环境变量（`ANYSCALE_SERVICE_URL`、`ANYSCALE_SERVICE_ROUTE`、`ANYSCALE_SERVICE_TOKEN`）。
- 有关更多详细信息，请参阅[Anyscale 文档](https://www.anyscale.com/get-started)。

我们需要安装 `openai` 包：

<CodeGroup>
```bash pip
pip install openai
```

```bash uv
uv add openai
```
</CodeGroup>

## LLM

请参阅[使用示例](/oss/integrations/llms/anyscale)。

```python
from langchain_community.llms.anyscale import Anyscale
```

## 嵌入模型 (Embeddings)

请参阅[使用示例](/oss/integrations/text_embedding/anyscale)。

```python
from langchain_community.embeddings import AnyscaleEmbeddings
```