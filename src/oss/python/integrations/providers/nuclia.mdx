---
title: Nuclia
---

>[Nuclia](https://nuclia.com) 可以自动索引您来自任何内部和外部源的非结构化数据，从而提供优化的搜索结果和生成式答案。它能够处理视频和音频转录、图像内容提取以及文档解析。

## 安装和设置

我们需要安装 `nucliadb-protos` 包，以便使用 `Nuclia Understanding API`。

<CodeGroup>
```bash pip
pip install nucliadb-protos
```

```bash uv
uv add nucliadb-protos
```
</CodeGroup>

我们需要有一个 `Nuclia 账户`。您可以在 [https://nuclia.cloud](https://nuclia.cloud) 免费创建一个，然后[创建一个 NUA 密钥](https://docs.nuclia.dev/docs/docs/using/understanding/intro)。

## 文档转换器 (Document Transformer)

### Nuclia

>`Nuclia Understanding API` 文档转换器会将文本拆分成段落和句子，识别实体，提供文本摘要，并为所有句子生成嵌入向量（embeddings）。

要使用 Nuclia 文档转换器，我们需要实例化一个 `NucliaUnderstandingAPI` 工具，并将 `enable_ml` 参数设置为 `True`：

```python
from langchain_community.tools.nuclia import NucliaUnderstandingAPI

nua = NucliaUnderstandingAPI(enable_ml=True)
```

请参阅[使用示例](/oss/integrations/document_transformers/nuclia_transformer)。

```python
from langchain_community.document_transformers.nuclia_text_transform import NucliaTextTransformer
```

## 文档加载器 (Document Loader)

### Nuclea 加载器

请参阅[使用示例](/oss/integrations/document_loaders/nuclia)。

```python
from langchain_community.document_loaders.nuclia import NucliaLoader
```

## 向量存储 (Vector Store)

### NucliaDB

我们需要安装一个 Python 包：

<CodeGroup>
```bash pip
pip install nuclia
```

```bash uv
uv add nuclia
```
</CodeGroup>

请参阅[使用示例](/oss/integrations/vectorstores/nucliadb)。

```python
from langchain_community.vectorstores.nucliadb import NucliaDB
```

## 工具 (Tools)

### Nuclia Understanding

请参阅[使用示例](/oss/integrations/tools/nuclia)。

```python
from langchain_community.tools.nuclia import NucliaUnderstandingAPI
```