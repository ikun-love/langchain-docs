---
title: DocArray
---

> [DocArray](https://docarray.jina.ai/) 是一个用于处理传输中嵌套、非结构化、多模态数据的库，这些数据包括文本、图像、音频、视频、3D 网格等。它使深度学习工程师能够使用 Pythonic API 高效地处理、嵌入、搜索、推荐、存储和传输多模态数据。

## 安装与设置

我们需要安装 `docarray` Python 包。

<CodeGroup>
```bash pip
pip install docarray
```

```bash uv
uv add docarray
```
</CodeGroup>

## 向量存储 (Vector Store)

LangChain 提供了对 `DocArray` 库中 `In-memory`（内存）和 `HNSW` 向量存储的访问。

查看一个 [使用示例](/oss/integrations/vectorstores/docarray_hnsw)。

```python
from langchain_community.vectorstores import DocArrayHnswSearch
```
查看一个 [使用示例](/oss/integrations/vectorstores/docarray_in_memory)。

```python
from langchain_community.vectorstores import DocArrayInMemorySearch
```

## 检索器 (Retriever)

查看一个 [使用示例](/oss/integrations/retrievers/docarray_retriever)。

```python
from langchain_community.retrievers import DocArrayRetriever
```