---
title: Nebius
---

所有与 Nebius AI Studio 相关的功

>[Nebius AI Studio](https://studio.nebius.ai/) 提供对各种最先进的大型语言模型和嵌入模型（embedding models）的 API 访问权限，以支持不同的使用场景。

## 安装和设置

可以通过 `pip` 安装 Nebius 集成：

<CodeGroup>
```bash pip
pip install langchain-nebius
```

```bash uv
uv add langchain-nebius
```
</CodeGroup>

要使用 Nebius AI Studio，您需要一个 API 密钥，该密钥可以从 [Nebius AI Studio](https://studio.nebius.ai/) 获取。API 密钥可以作为初始化参数 `api_key` 传入，或者设置为环境变量 `NEBIUS_API_KEY`。

```python
import os
os.environ["NEBIUS_API_KEY"] = "YOUR-NEBIUS-API-KEY"
```

### 可用模型

支持的模型完整列表可以在 [Nebius AI Studio 文档](https://studio.nebius.com/) 中找到。


## 聊天模型 (Chat Models)

### ChatNebius

`ChatNebius` 类允许您与 Nebius AI Studio 的聊天模型进行交互。

参见 [使用示例](/oss/integrations/chat/nebius)。

```python
from langchain_nebius import ChatNebius

# 初始化聊天模型
chat = ChatNebius(
    model="Qwen/Qwen3-30B-A3B-fast",  # 从可用模型中选择
    temperature=0.6,
    top_p=0.95
)
```

## 嵌入模型 (Embedding Models)

### NebiusEmbeddings

`NebiusEmbeddings` 类允许您使用 Nebius AI Studio 的嵌入模型来生成向量嵌入。

参见 [使用示例](/oss/integrations/text_embedding/nebius)。

```python
from langchain_nebius import NebiusEmbeddings

# 初始化嵌入模型
embeddings = NebiusEmbeddings(
    model="BAAI/bge-en-icl"  # 默认嵌入模型
)
```

## 检索器 (Retrievers)

### NebiusRetriever

`NebiusRetriever` 实现了高效的相似性搜索，它利用 Nebius AI Studio 提供的嵌入模型，对文档集进行语义搜索。

参见 [使用示例](/oss/integrations/retrievers/nebius)。

```python
from langchain_core.documents import Document
from langchain_nebius import NebiusEmbeddings, NebiusRetriever

# 创建示例文档
docs = [
    Document(page_content="Paris is the capital of France"),
    Document(page_content="Berlin is the capital of Germany"),
]

# 初始化嵌入模型
embeddings = NebiusEmbeddings()

# 创建检索器
retriever = NebiusRetriever(
    embeddings=embeddings,
    docs=docs,
    k=2  # 返回的文档数量
)
```

## 工具 (Tools)

### NebiusRetrievalTool

`NebiusRetrievalTool` 允许您基于 `NebiusRetriever` 为智能体（Agents）创建一个工具。

```python
from langchain_nebius import NebiusEmbeddings, NebiusRetriever, NebiusRetrievalTool
from langchain_core.documents import Document

# 创建示例文档
docs = [
    Document(page_content="Paris is the capital of France and has the Eiffel Tower"),
    Document(page_content="Berlin is the capital of Germany and has the Brandenburg Gate"),
]

# 创建嵌入模型和检索器
embeddings = NebiusEmbeddings()
retriever = NebiusRetriever(embeddings=embeddings, docs=docs)

# 创建检索工具
tool = NebiusRetrievalTool(
    retriever=retriever,
    name="nebius_search",
    description="Search for information about European capitals"
)
```