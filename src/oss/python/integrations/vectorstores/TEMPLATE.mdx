---
title: (MODULE_NAME)向量存储
---

如何使用此 Python 向量存储模板：

- [ ] 将 `(MODULE_NAME)` 替换为模块的名称，例如 Anthropic、OpenAI 等（使用 `command + F` 组合键会更方便）。
- [ ] 更新链接以指向正确的模块。
- [ ] 根据需要更新 PyPi/注册表包的名称。
- [ ] 根据需要更新 API 密钥的环境变量名称。

模板从以下行开始...

本指南为快速开始使用 `(MODULE_NAME)` [向量存储](/oss/integrations/vectorstores#overview) 提供了概述。有关 `(MODULE_NAME)VectorStore` 所有功能、参数和配置的详细列表，请前往 `(MODULE_NAME)VectorStore` API 参考文档（[LINK]）。

## 设置 (Setup)

要访问 `(MODULE_NAME)` 向量存储，您需要创建一个（或拥有一个）`(MODULE_NAME)` 账户，获取一个 API 密钥，并安装 `langchain-(MODULE_NAME)` 集成包。

### 凭证 (Credentials)

```python 设置 API 密钥 icon="key"
import getpass
import os

if "(MODULE_NAME)_API_KEY" not in os.environ:
    os.environ["(MODULE_NAME)_API_KEY"] = getpass.getpass("输入您的 (MODULE_NAME) API 密钥: ")
```

要启用模型调用的自动化的 <Tooltip tip="记录模型执行的每一步，以便于调试和改进">跟踪 (tracing)</Tooltip>，请设置您的 [LangSmith](https://docs.langchain.com/langsmith/home) API 密钥：

```python 启用跟踪 icon="flask"
os.environ["LANGSMITH_API_KEY"] = getpass.getpass("输入您的 LangSmith API 密钥: ")
os.environ["LANGSMITH_TRACING"] = "true"
```

### 安装 (Installation)

LangChain `(MODULE_NAME)` 集成位于 `langchain-(MODULE_NAME)` 包中：

<CodeGroup>
    ```python pip
    pip install -U langchain-(MODULE_NAME)
    ```
    ```python uv
    uv add langchain-(MODULE_NAME)
    ```
</CodeGroup>

---

## 实例化 (Instantiation)

现在我们可以实例化我们的模型对象并生成响应：

```python 初始化向量存储 icon="database"
from langchain_(MODULE_NAME).vectorstores import (MODULE_NAME)VectorStore

vector_store = (MODULE_NAME)VectorStore(embeddings=embeddings)
```

---

## 管理向量存储 (Manage vector store)

### 添加条目 (Add items)

```python 添加文档 icon="folder-plus"
from langchain_core.documents import Document

document_1 = Document(page_content="foo", metadata={"source": "https://example.com"})
document_2 = Document(page_content="bar", metadata={"source": "https://example.com"})
document_3 = Document(page_content="baz", metadata={"source": "https://example.com"})
documents = [document_1, document_2, document_3]

vector_store.add_documents(documents=documents, ids=["1", "2", "3"])
```

### 更新条目 (Update items)

```python 按 ID 更新文档 icon="pencil"
updated_document = Document(
    page_content="qux", metadata={"source": "https://another-example.com"}
)

vector_store.update_documents(document_id="1", document=updated_document)
```

### 删除条目 (Delete items)

```python 按 ID 删除文档 icon="trash"
vector_store.delete(ids=["3"])
```

---

## 查询向量存储 (Query vector store)

一旦创建了向量存储并将相关文档添加进去，您很可能希望在链或代理运行时对其进行查询。

### 直接查询 (Directly)

可以按以下方式执行简单的相似性搜索：

```python 相似性搜索 icon="folder-tree"
results = vector_store.similarity_search(
    query="thud", k=1, filter={"source": "https://another-example.com"}
)
for doc in results:
    print(f"* {doc.page_content} [{doc.metadata}]")
```

如果您想执行相似性搜索并接收相应的分数，可以运行：

```python 带分数的相似性搜索 icon="star-half-stroke"
results = vector_store.similarity_search_with_score(
    query="thud", k=1, filter={"source": "https://example.com"}
)
for doc, score in results:
    print(f"* [SIM={score:3f}] {doc.page_content} [{doc.metadata}]")
```

### 转换为检索器 (By turning into retriever)

您也可以将向量存储转换为检索器（retriever），以便在链中使用更方便。

```python 创建检索器 icon="robot"
retriever = vector_store.as_retriever(search_type="mmr", search_kwargs={"k": 1})
retriever.invoke("thud")
```

---

## 用于检索增强生成 (RAG) 的用法

<Card
    title="(MODULE_NAME) 向量存储 RAG 指南"
    href="/oss/langchain/rag"
    icon="magnifying-glass"
    arrow
>
    关于如何使用此向量存储进行检索增强生成 (RAG) 的指南
</Card>

---

## TODO: 此向量存储的任何特定功能

如果与当前实现不相关，请删除此部分。

请参考现有向量存储文档以获取示例，例如：

- [Qdrant](/oss/integrations/vectorstores/qdrant)
- [FAISS](/oss/integrations/vectorstores/faiss)
- [Chroma](/oss/integrations/vectorstores/chroma)

示例：
- <Icon icon="ruler-horizontal" size={16}/> 距离函数 (Distance functions)
- <Icon icon="chart-simple" size={16}/> 相关性 (Relevance)
- <Icon icon="filter" size={16}/> 过滤 (Filtering)
- <Icon icon="server" size={16} /> 外部存储选项 (External storage options)
- <Icon icon="tags" size={16} /> 索引 (Indexing)
- <Icon icon="code-merge" size={16} /> 合并 (Merging)

---

## API 参考 (API reference)

有关所有 `(MODULE_NAME)VectorStore` 功能和配置的详细文档，请前往 [API 参考](<LINK>)。