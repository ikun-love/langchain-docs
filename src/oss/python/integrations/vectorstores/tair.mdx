---
title: Tair
---

>[Tair](https://www.alibabacloud.com/help/en/tair/latest/what-is-tair) 是阿里云（`Alibaba Cloud`）开发的一款云原生内存数据库服务。
它提供丰富的数据模型和企业级能力，以支持您的实时在线场景，同时完全兼容开源的 `Redis`。`Tair` 还引入了基于新型非易失性内存（NVM）存储介质的持久化内存优化实例。

本示例（Notebook）展示了如何使用与 `Tair` 向量数据库相关的各项功能。

您需要安装 `langchain-community`：`pip install -qU langchain-community` 才能使用此集成。

要运行代码，您需要启动并运行一个 `Tair` 实例。

```python
from langchain_community.embeddings.fake import FakeEmbeddings
from langchain_community.vectorstores import Tair
from langchain_text_splitters import CharacterTextSplitter
```

```python
from langchain_community.document_loaders import TextLoader

loader = TextLoader("../../how_to/state_of_the_union.txt")
documents = loader.load()
text_splitter = CharacterTextSplitter(chunk_size=1000, chunk_overlap=0)
docs = text_splitter.split_documents(documents)

embeddings = FakeEmbeddings(size=128)
```

可以通过设置 `TAIR_URL` 环境变量来连接到 Tair：

```
export TAIR_URL="redis://{username}:{password}@{tair_address}:{tair_port}"
```

或者使用关键字参数 `tair_url` 进行连接。

然后将文档和嵌入向量存储到 Tair 中。

```python
tair_url = "redis://localhost:6379"

# 如果索引已存在，先删除它
Tair.drop_index(tair_url=tair_url)

vector_store = Tair.from_documents(docs, embeddings, tair_url=tair_url)
```

查询相似文档。

```python
query = "What did the president say about Ketanji Brown Jackson"
docs = vector_store.similarity_search(query)
docs[0]
```

Tair 混合搜索索引构建

```python
# 如果索引已存在，先删除它
Tair.drop_index(tair_url=tair_url)

vector_store = Tair.from_documents(
    docs, embeddings, tair_url=tair_url, index_params={"lexical_algorithm": "bm25"}
)
```

Tair 混合搜索

```python
query = "What did the president say about Ketanji Brown Jackson"
# hybrid_ratio: 0.5 表示混合搜索，0.9999 表示向量搜索，0.0001 表示文本搜索
kwargs = {"TEXT": query, "hybrid_ratio": 0.5}
docs = vector_store.similarity_search(query, **kwargs)
docs[0]
```