```markdown
---
title: Trubrics
---

>[Trubrics](https://trubrics.com) 是一个 LLM（大型语言模型）用户分析平台，它允许您收集、分析和管理用户针对 AI 模型的提示词（Prompts）和反馈。
>
>有关 `Trubrics` 的更多信息，请查看 [Trubrics 仓库](https://github.com/trubrics/trubrics-sdk)。

在本指南中，我们将介绍如何设置 `TrubricsCallbackHandler`。

## 安装与设置

```python
pip install -qU  trubrics langchain langchain-community
```

### 获取 Trubrics 凭证

如果您还没有 Trubrics 账户，请在此 [链接](https://trubrics.streamlit.app/) 创建一个。在本教程中，我们将使用账户创建时自带的 `default` 项目。

现在将您的凭证设置为环境变量：

```python
import os

os.environ["TRUBRICS_EMAIL"] = "***@***"
os.environ["TRUBRICS_PASSWORD"] = "***"
```

```python
from langchain_community.callbacks.trubrics_callback import TrubricsCallbackHandler
```

### 用法

`TrubricsCallbackHandler` 可以接收各种可选参数。有关可以传递给 Trubrics 提示词的 kwargs（关键字参数），请参阅 [此处](https://trubrics.github.io/trubrics-sdk/platform/user_prompts/#saving-prompts-to-trubrics)。

```python
class TrubricsCallbackHandler(BaseCallbackHandler):

    """
    Trubrics 的回调处理器（Callback handler）。

    Args:
        project: 一个 trubrics 项目，默认为 "default"
        email: trubrics 账户邮箱，也可以在环境变量中设置
        password: trubrics 账户密码，也可以在环境变量中设置
        **kwargs: 所有其他 kwargs 都将被解析并设置为 trubrics 提示词变量，或者添加到 `metadata` 字典中
    """
```

## 示例

以下是如何将 `TrubricsCallbackHandler` 与 LangChain 的 [LLM](/oss/langchain/models) 或 [Chat Models](/oss/langchain/models) 结合使用的两个示例。我们将使用 OpenAI 模型，因此请在此处设置您的 `OPENAI_API_KEY`：

```python
os.environ["OPENAI_API_KEY"] = "sk-***"
```

### 1. 与 LLM 结合使用

```python
from langchain_openai import OpenAI
```

```python
llm = OpenAI(callbacks=[TrubricsCallbackHandler()])
```

```output
2023-09-26 11:30:02.149 | INFO     | trubrics.platform.auth:get_trubrics_auth_token:61 - 用户 jeff.kayne@trubrics.com 已通过身份验证。
```

```python
res = llm.generate(["讲个笑话", "给我写首诗"])
```

```output
2023-09-26 11:30:07.760 | INFO     | trubrics.platform:log_prompt:102 - 用户提示词已保存到 Trubrics。
2023-09-26 11:30:08.042 | INFO     | trubrics.platform:log_prompt:102 - 用户提示词已保存到 Trubrics。
```

```python
print("--> GPT 的笑话: ", res.generations[0][0].text)
print()
print("--> GPT 的诗: ", res.generations[1][0].text)
```

```output
--> GPT 的笑话:

Q: 鱼撞到墙上说了什么？
A: 坝（Dam）！ (注：Dam 在英文中意为“水坝”，与 “Damn” [该死] 发音相近，此处是双关笑话)

--> GPT 的诗:

反思之诗

我今夜站立，
头顶是繁星点点。
我感受到深刻的联系，
与世界及其完美。

内心的清晰，
空气如此宁静。
我的思绪充满了平静，
我得以解脱。

过去与现在，
我的想法构造出愉悦的情感。
我的心充满了喜悦，
我的灵魂像玩具般飞翔。

我反思我的生活，
以及我所做的选择。
我的挣扎与艰辛，
我所学到的教训。

未来是迷，
但我准备好迎接飞跃。
我准备好引领，
创造我自己的命运。
```

### 2. 与聊天模型（Chat Model）结合使用

```python
from langchain.messages import HumanMessage, SystemMessage
from langchain_openai import ChatOpenAI
```

```python
chat_llm = ChatOpenAI(
    callbacks=[
        TrubricsCallbackHandler(
            project="default",
            tags=["chat model"],
            user_id="user-id-1234",
            some_metadata={"hello": [1, 2]},
        )
    ]
)
```

```python
chat_res = chat_llm.invoke(
    [
        SystemMessage(content="你的所有回答必须是关于 OpenAI 的。"),
        HumanMessage(content="讲个笑话"),
    ]
)
```

```output
2023-09-26 11:30:10.550 | INFO     | trubrics.platform:log_prompt:102 - 用户提示词已保存到 Trubrics。
```

```python
print(chat_res.content)
```

```output
OpenAI 电脑为什么要去参加派对？

因为它想见见它的 AI 朋友，并“字节”(byte) 找点乐子！ (注：byte 与 bite [咬/吃] 同音)
```

```

```