---
title: Chat(MODULE_NAME)
---

如何使用这个 Python 聊天模型模板：

- [ ] 将 (MODULE_NAME) 替换为模块的名称，例如 Anthropic, OpenAI 等（使用 `command + F` 可以快速查找和替换）。
- [ ] 更新链接，使其指向正确的模块。
- [ ] 在 `details` 和 `features` 表格下方，更新 ✅/❌ 以反映聊天模型的实际能力。
- [ ] 如果有需要，请更新 PyPi/注册表包的名称。
- [ ] 如果有需要，请更新 API 密钥的环境变量名称。

模板从以下行开始...

本指南提供了启动 (MODULE_NAME) [聊天模型](/oss/langchain/models)的快速概览。要查看 Chat(MODULE_NAME) 所有功能、参数和配置的详细列表，请转至 [Chat(MODULE_NAME) API 参考](https://python.langchain.com/api_reference/(MODULE_NAME)/chat_models/langchain_(MODULE_NAME).chat_models.Chat(MODULE_NAME).html)。

## 概览

### 详情

| 类 (Class) | 包 (Package) | 本地 (Local) | 可序列化 (Serializable) | [JS 支持](https://js.langchain.com/docs/integrations/chat/anthropic) | 下载次数 (Downloads) | 版本 (Version) |
| :--- | :--- | :---: | :---: |  :---: | :---: | :---: |
| [Chat(MODULE_NAME)](https://python.langchain.com/api_reference/(MODULE_NAME)/chat_models/langchain_(MODULE_NAME).chat_models.Chat(MODULE_NAME).html) | [langchain-(MODULE_NAME)](https://python.langchain.com/api_reference/anthropic/index.html) | ✅/❌ | beta/❌ | ✅/❌ | ![PyPI - Downloads](https://img.shields.io/pypi/dm/langchain-(MODULE_NAME)?style=flat-square&label=%20) | ![PyPI - Version](https://img.shields.io/pypi/v/langchain-(MODULE_NAME)?style=flat-square&label=%20) |

### 功能

| [工具调用](/oss/langchain/tools) | [结构化输出](/oss/langchain/structured-output/) | JSON 模式 | [图像输入](/oss/langchain/messages#multimodal) | [音频输入](/oss/langchain/messages#multimodal) | [视频输入](/oss/langchain/messages#multimodal) | [Token 级流式传输](/oss/langchain/streaming/) | 原生异步 (Native async) | [Token 使用量](/oss/langchain/models#token-usage) | [Logprobs](/oss/langchain/models#log-probabilities) |
| :---: | :---: | :---: | :---: |  :---: | :---: | :---: | :---: | :---: | :---: |
| ✅/❌ | ✅/❌ | ✅/❌ | ✅/❌ | ✅/❌ | ✅/❌ | ✅/❌ | ✅/❌ | ✅/❌ | ✅❌ |

---

## 设置 (Setup)

要访问 (MODULE_NAME) 模型，您需要创建一个 (MODULE_NAME) 帐户，获取一个 API 密钥，并安装 `langchain-(MODULE_NAME)` 集成包。

### 凭证 (Credentials)

```python Set API key icon="key"
import getpass
import os

if "(MODULE_NAME)_API_KEY" not in os.environ:
    os.environ["(MODULE_NAME)_API_KEY"] = getpass.getpass("Enter your (MODULE_NAME) API key: ")
```

要启用模型调用的自动化 <Tooltip tip="Log each step of a model's execution to debug and improve it">跟踪</Tooltip>（追踪），请设置您的 [LangSmith](https://docs.smith.langchain.com/) API 密钥：

```python Enable tracing icon="flask"
os.environ["LANGSMITH_API_KEY"] = getpass.getpass("Enter your LangSmith API key: ")
os.environ["LANGSMITH_TRACING"] = "true"
```

### 安装 (Installation)

LangChain (MODULE_NAME) 集成位于 `langchain-(MODULE_NAME)` 包中：

<CodeGroup>
    ```python pip
    pip install -U langchain-(MODULE_NAME)
    ```
    ```python uv
    uv add langchain-(MODULE_NAME)
    ```
</CodeGroup>

---

## 实例化 (Instantiation)

现在我们可以实例化模型对象并生成响应：

```python Initialize chat model icon="robot"
from langchain_(MODULE_NAME) import Chat(MODULE_NAME)

model = Chat(MODULE_NAME)(
    model="model-name",
    temperature=0,
    timeout=None,
    max_tokens=1024,
    max_retries=2,
    # 其他参数 - 有关完整列表，请参阅 API 参考
)
```

---

## 调用 (Invocation)

<CodeGroup>
    ```python Dictionary format icon="book"
    messages = [
        {"role": "system", "content": "You are a poetry expert"},
        {"role": "user", "content": "Write a haiku about spring"},
    ]
    response = model.invoke(messages)
    print(response)
    ```
    ```python Message objects icon="message"
    from langchain.messages import SystemMessage, HumanMessage, AIMessage

    messages = [
        SystemMessage("You are a poetry expert"),
        HumanMessage("Write a haiku about spring"),
        AIMessage("Cherry blossoms bloom...")
    ]
    response = model.invoke(messages)
    ```
</CodeGroup>

```text Response object icon="terminal"
TODO - replace with response.model_dump_json(indent=2) or similar
```

```python Text content icon="i-cursor"
print(response.text)

# TODO - replace with the output of response.text
```

```python Content Blocks icon="shapes"
print(response.content_blocks)

# TODO - replace with the output of response.content_blocks
```

<Tip>
    完整的指南可在 [聊天模型调用类型](/oss/langchain/models#invocation)、[消息类型](/oss/langchain/messages#message-types) 和 [内容块](/oss/langchain/messages#standard-content-blocks) 处找到。
</Tip>

## TODO: 此模型的任何特定功能

如果无关则删除。

请参考现有模型文档中的示例，例如：

- [ChatAnthropic](/oss/integrations/chat/anthropic)
- [ChatOpenAI](/oss/integrations/chat/openai)
- [ChatGenAI](/oss/integrations/chat/google_generative_ai)

示例：
- <Icon icon="wrench" size={16}/> 工具调用
- <Icon icon="brain" size={16}/> 逻辑推理输出/扩展推理
- <Icon icon="bullhorn" size={16}/> 冗长输出 (Verbosity)
- <Icon icon="quote-right" size={16}/> 引用 (Citations)
- <Icon icon="terminal" size={16}/> 内置工具
    - <Icon icon="clipboard-question" size={16}/> 网页搜索
    - <Icon icon="code" size={16}/> 代码执行
    - <Icon icon="rss" size={16}/> 远程 MCP
- <Icon icon="photo-film" size={16}/> 多模态输入/输出
- <Icon icon="database" size={16}/> 缓存 (Caching)
- <Icon icon="link" size={16}/> 链式调用 (Chaining)
- <Icon icon="wifi" size={16}/> 流式传输使用元数据
- <Icon icon="vial" size={16}/> 微调 (Fine-tuning)
- <Icon icon="microchip" size={16}/> Flex 处理
- <Icon icon="timeline" size={16}/> 自定义基础 URL 或代理行为

---

## API 参考

有关 Chat(MODULE_NAME) 所有功能和配置的详细文档，请参阅 [API 参考](<LINK>)。