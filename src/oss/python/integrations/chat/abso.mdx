---
title: ChatAbso
---

这将帮助您开始使用 ChatAbso [聊天模型](https://python.langchain.com/docs/concepts/chat_models/)。有关所有 ChatAbso 功能和配置的详细文档，请访问 [API 参考](https://python.langchain.com/api_reference/en/latest/chat_models/langchain_abso.chat_models.ChatAbso.html)。

- 您可以在 [此处] ([abso.ai](https://abso.ai)) 找到 Abso 路由器的完整文档

## 概述 (Overview)

### 集成详情 (Integration details)

| 类 (Class) | 包 (Package) | 本地 (Local) | 可序列化 (Serializable) | [JS 支持](https://js.langchain.com/docs/integrations/chat/abso) | 下载量 (Downloads) | 版本 (Version) |
| :--- | :--- | :---: | :---: | :---: | :---: | :---: |
| [ChatAbso](https://python.langchain.com/api_reference/en/latest/chat_models/langchain_abso.chat_models.ChatAbso.html) | [langchain-abso](https://python.langchain.com/api_reference/en/latest/abso_api_reference.html) | ❌ | ❌ | ❌ | ![PyPI - Downloads](https://img.shields.io/pypi/dm/langchain-abso?style=flat-square&label=%20) | ![PyPI - Version](https://img.shields.io/pypi/v/langchain-abso?style=flat-square&label=%20) |

## 设置 (Setup)

要访问 ChatAbso 模型，您需要创建一个 OpenAI 账户，获取一个 API 密钥，并安装 `langchain-abso` 集成包。

### 凭证 (Credentials)

- TODO: 使用相关信息更新。

前往 (TODO: link)，注册 ChatAbso 并生成 API 密钥。完成此操作后，设置 `ABSO_API_KEY` 环境变量：

```python
import getpass
import os

if not os.getenv("OPENAI_API_KEY"):
    os.environ["OPENAI_API_KEY"] = getpass.getpass("Enter your OpenAI API key: ")
```

### 安装 (Installation)

LangChain ChatAbso 集成位于 `langchain-abso` 包中：

```python
pip install -qU langchain-abso
```

## 实例化 (Instantiation)

现在我们可以实例化模型对象并生成聊天补全：

```python
from langchain_abso import ChatAbso

llm = ChatAbso(fast_model="gpt-4o", slow_model="o3-mini")
```

## 调用 (Invocation)

```python
messages = [
    (
        "system",
        "You are a helpful assistant that translates English to French. Translate the user sentence.",
    ),
    ("human", "I love programming."),
]
ai_msg = llm.invoke(messages)
ai_msg
```

```python
print(ai_msg.content)
```

---

## API 参考 (API reference)

有关所有 ChatAbso 功能和配置的详细文档，请访问 API 参考：[python.langchain.com/api_reference/en/latest/chat_models/langchain_abso.chat_models.ChatAbso.html](https://python.langchain.com/api_reference/en/latest/chat_models/langchain_abso.chat_models.ChatAbso.html)