---
title: Clova 嵌入
---

[Clova](https://api.ncloud-docs.com/docs/ai-naver-clovastudio-summary) 提供了一个嵌入（Embeddings）服务。

本示例将介绍如何使用 LangChain 与 Clova 推理服务进行文本嵌入交互。

```python
import os

os.environ["CLOVA_EMB_API_KEY"] = ""
os.environ["CLOVA_EMB_APIGW_API_KEY"] = ""
os.environ["CLOVA_EMB_APP_ID"] = ""
```

```python
from langchain_community.embeddings import ClovaEmbeddings
```

```python
embeddings = ClovaEmbeddings()
```

```python
query_text = "This is a test query."
query_result = embeddings.embed_query(query_text)
```

```python
document_text = ["This is a test doc1.", "This is a test doc2."]
document_result = embeddings.embed_documents(document_text)
```