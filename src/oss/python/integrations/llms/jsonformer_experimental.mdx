---
title: JSONFormer
---

[JSONFormer](https://github.com/1rgs/jsonformer) 是一个库，它包装了本地的 Hugging Face pipeline 模型，用于对 JSON Schema 的子集进行结构化解码。

其工作原理是先填充结构化 token，然后从模型中采样内容 token。

**警告 - 此模块仍处于实验阶段**

```python
pip install -qU  jsonformer > /dev/null
```

### Hugging Face 基线

首先，让我们通过检查没有结构化解码的模型输出，来建立一个定性的基线。

```python
import logging

logging.basicConfig(level=logging.ERROR)
```

```python
import json
import os

import requests
from langchain.tools import tool

HF_TOKEN = os.environ.get("HUGGINGFACE_API_KEY")


@tool
def ask_star_coder(query: str, temperature: float = 1.0, max_new_tokens: float = 250):
    """关于编码问题询问 BigCode StarCoder 模型。"""
    url = "https://api-inference.huggingface.co/models/bigcode/starcoder"
    headers = {
        "Authorization": f"Bearer {HF_TOKEN}",
        "content-type": "application/json",
    }
    payload = {
        "inputs": f"{query}\n\nAnswer:",
        "temperature": temperature,
        "max_new_tokens": int(max_new_tokens),
    }
    response = requests.post(url, headers=headers, data=json.dumps(payload))
    response.raise_for_status()
    return json.loads(response.content.decode("utf-8"))
```

```python
prompt = """你必须使用 JSON 格式进行响应，包含一个 action（动作）和一个 action_input（动作输入）。
你可以向 'ask_star_coder' 寻求编码问题的帮助。

{arg_schema}

示例
----
Human: "那么关于 GIL 都是怎么回事？"
AI Assistant:{{
  "action": "ask_star_coder",
  "action_input": {{"query": "什么是 GIL?", "temperature": 0.0, "max_new_tokens": 100}}"
}}
Observation: "GIL 是 Python 的全局解释器锁"
Human: "你能用 LISP 写一个计算器程序吗？"
AI Assistant:{{
  "action": "ask_star_coder",
  "action_input": {{"query": "用 LISP 编写一个计算器程序", "temperature": 0.0, "max_new_tokens": 250}}
}}
Observation: "(defun add (x y) (+ x y))\n(defun sub (x y) (- x y ))"
Human: "SVM 和 LLM 有什么区别？"
AI Assistant:{{
  "action": "ask_star_coder",
  "action_input": {{"query": "SGD 和 SVM 有什么区别?", "temperature": 1.0, "max_new_tokens": 250}}
}}
Observation: "SGD 代表随机梯度下降，而 SVM 是支持向量机。"

开始！ 尽你所能回答人类的问题。
------
Human: '迭代器（iterator）和可迭代对象（iterable）有什么区别？'
AI Assistant:""".format(arg_schema=ask_star_coder.args)
```

```python
from langchain_huggingface import HuggingFacePipeline
from transformers import pipeline

hf_model = pipeline(
    "text-generation", model="cerebras/Cerebras-GPT-590M", max_new_tokens=200
)

original_model = HuggingFacePipeline(pipeline=hf_model)

generated = original_model.predict(prompt, stop=["Observation:", "Human:"])
print(generated)
```

```output
Setting `pad_token_id` to `eos_token_id`:50256 for open-end generation.
```
```output
 'What's the difference between an iterator and an iterable?'
```

***这不怎么令人印象深刻，对吧？它完全没有遵循 JSON 格式！让我们使用结构化解码再试一次。***

## JSONFormer LLM 包装器

我们再试一次，这次向模型提供 Action 输入的 JSON Schema。

```python
decoder_schema = {
    "title": "解码 Schema",
    "type": "object",
    "properties": {
        "action": {"type": "string", "default": ask_star_coder.name},
        "action_input": {
            "type": "object",
            "properties": ask_star_coder.args,
        },
    },
}
```

```python
from langchain_experimental.llms import JsonFormer

json_former = JsonFormer(json_schema=decoder_schema, pipeline=hf_model)
```

```python
results = json_former.predict(prompt, stop=["Observation:", "Human:"])
print(results)
```

```output
{"action": "ask_star_coder", "action_input": {"query": "What's the difference between an iterator and an iter", "temperature": 0.0, "max_new_tokens": 50.0}}
```

**瞧！没有解析错误。**

```python

```