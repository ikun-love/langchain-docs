---
title: Xorbits 推理 (Xinference)
---

[Xinference](https://github.com/xorbitsai/inference) 是一个功能强大且用途广泛的库，专为服务大型语言模型（LLMs）、语音识别模型和多模态模型而设计，即使在您的笔记本电脑上也能运行。它支持多种与 GGML 兼容的模型，例如 chatglm、baichuan、whisper、vicuna、orca 等等。本示例 Notebook 展示了如何将 Xinference 与 LangChain 结合使用。

## 安装

通过 PyPI 安装 `Xinference`：

```python
pip install -qU  "xinference[all]"
```

## 在本地或分布式集群中部署 Xinference

对于本地部署，运行 `xinference` 命令。

要在集群中部署 Xinference，首先使用 `xinference-supervisor` 启动一个 Xinference 监督器（Supervisor）。您也可以使用 `-p` 选项指定端口（默认为 9997）和 `-H` 选项指定主机名。

然后，在您希望运行工作节点（Worker）的每台服务器上，使用 `xinference-worker` 启动 Xinference 工作节点。

您可以查阅 [Xinference](https://github.com/xorbitsai/inference) 的 README 文件以获取更多信息。

## 包装器 (Wrapper)

要将 Xinference 与 LangChain 结合使用，您需要先启动一个模型。您可以使用命令行界面（CLI）来完成此操作：

```python
!xinference launch -n vicuna-v1.3 -f ggmlv3 -q q4_0
```

```output
Model uid: 7167b2b0-2a04-11ee-83f0-d29396a3f064
```

系统会返回一个模型 UID，供您后续使用。现在您就可以将 Xinference 与 LangChain 配合使用了：

```python
from langchain_community.llms import Xinference

llm = Xinference(
    server_url="http://0.0.0.0:9997", model_uid="7167b2b0-2a04-11ee-83f0-d29396a3f064"
)

llm(
    prompt="Q: 我们可以去法国首都不参观什么地方? A:",
    generate_config={"max_tokens": 1024, "stream": True},
)
```

```output
' You can visit the Eiffel Tower, Notre-Dame Cathedral, the Louvre Museum, and many other historical sites in Paris, the capital of France.'
```

### 与 LLMChain 集成

```python
from langchain.chains import LLMChain
from langchain_core.prompts import PromptTemplate

# 模板定义了我们想要查询的国家
template = "Where can we visit in the capital of {country}?"

prompt = PromptTemplate.from_template(template)

llm_chain = LLMChain(prompt=prompt, llm=llm)

generated = llm_chain.run(country="France")
print(generated)
```

```output
A: You can visit many places in Paris, such as the Eiffel Tower, the Louvre Museum, Notre-Dame Cathedral, the Champs-Elysées, Montmartre, Sacré-Cœur, and the Palace of Versailles.
```

最后，当您不再需要使用该模型时，请务必终止它：

```python
!xinference terminate --model-uid "7167b2b0-2a04-11ee-83f0-d29396a3f064"
```