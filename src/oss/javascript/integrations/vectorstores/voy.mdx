---
title: Voy
---

[Voy](https://github.com/tantaraio/voy) 是一个使用 Rust 编写的 WASM 向量相似性搜索引擎。
它支持在浏览器等非 Node.js 环境中运行。您可以在 LangChain.js 中将 Voy 用作向量存储（vector store）。

### 安装 Voy

<Tip>
有关安装 LangChain 包的通用说明，请参阅 [本文档部分](/oss/langchain/install)。
</Tip>

```bash npm
npm install @langchain/openai voy-search @langchain/community @langchain/core
```

## 用法 (Usage)

```typescript
import { VoyVectorStore } from "@langchain/community/vectorstores/voy";
import { Voy as VoyClient } from "voy-search";
import { OpenAIEmbeddings } from "@langchain/openai";
import { Document } from "@langchain/core/documents";

// 使用该库创建 Voy 客户端。
const voyClient = new VoyClient();
// 创建嵌入模型 (embeddings)。
const embeddings = new OpenAIEmbeddings();
// 创建 Voy 存储实例。
const store = new VoyVectorStore(voyClient, embeddings);

// 添加两条带有某些元数据 (metadata) 的文档。
await store.addDocuments([
  new Document({
    pageContent: "How has life been treating you?",
    metadata: {
      foo: "Mike",
    },
  }),
  new Document({
    pageContent: "And I took it personally...",
    metadata: {
      foo: "Testing",
    },
  }),
]);

const model = new OpenAIEmbeddings();
const query = await model.embedQuery("And I took it personally");

// 执行相似性搜索。
const resultsWithScore = await store.similaritySearchVectorWithScore(query, 1);

// 打印结果。
console.log(JSON.stringify(resultsWithScore, null, 2));
/*
  [
    [
      {
        "pageContent": "And I took it personally...",
        "metadata": {
          "foo": "Testing"
        }
      },
      0
    ]
  ]
*/
```

## 相关内容

- 向量存储 [概念指南](/oss/integrations/vectorstores)
- 向量存储 [操作指南](/oss/integrations/vectorstores)