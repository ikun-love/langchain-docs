---
title: Tavily 提取
---

[Tavily](https://tavily.com/) 是一个专为 AI 代理（LLM）构建的搜索引擎，它能够快速交付实时、准确且基于事实的搜索结果。Tavily 提供了四个核心端点，其中 **Extract（提取）** 端点可以从给定的 URL 中提供原始的提取内容。

本指南将快速介绍如何开始使用 Tavily 的 [工具](/oss/integrations/tools/)。要完整了解 Tavily 工具，您可以在 [API 参考](https://v03.api.js.langchain.com/modules/_langchain_tavily.html) 中找到更详细的文档。

## 概述

### 集成详情

| 类 (Class) | 包 (Package) | [Python 支持](https://python.langchain.com/docs/integrations/tools/tavily_extract/) | 版本 (Version) |
| :--- | :--- | :---: | :---: |
| [TavilyExtract](https://api.js.langchain.com/classes/_langchain_tavily.TavilyExtract.html) | [`@langchain/tavily`](https://www.npmjs.com/package/@langchain/tavily) | ✅ |  ![NPM - Version](https://img.shields.io/npm/v/@langchain/tavily?style=flat-square&label=%20&) |

## 设置

该集成位于 `@langchain/tavily` 包中，您可以通过如下方式安装：

<CodeGroup>
```bash npm
npm install @langchain/tavily @langchain/core
```
```bash yarn
yarn add @langchain/tavily @langchain/core
```
```bash pnpm
pnpm add @langchain/tavily @langchain/core
```
</CodeGroup>

### 凭证 (Credentials)

请在 [此处](https://app.tavily.com) 设置您的 API 密钥，并将其作为名为 `TAVILY_API_KEY` 的环境变量进行配置。

```typescript
process.env.TAVILY_API_KEY = "YOUR_API_KEY"
```

为了获得最佳的可观测性，设置 [LangSmith](https://smith.langchain.com/) 也是很有帮助的（但非必需）：

```typescript
process.env.LANGSMITH_TRACING="true"
process.env.LANGSMITH_API_KEY="your-api-key"
```

## 实例化

您可以像下面这样导入并实例化 `TavilyExtract` 工具：

```typescript
import { TavilyExtract } from "@langchain/tavily";

const tool = new TavilyExtract({
  extractDepth: "basic",
  includeImages: false,
});
```

## 调用 (Invocation)

### [直接使用参数调用](/oss/langchain/tools)

Tavily Extract 工具在调用时接受以下参数：

* `urls` (必需): 需要从中提取内容的 URL 列表。

* `extractDepth` 和 `includeImages` 也可以在调用时设置。

```typescript
await tool.invoke({
  "urls": ["https://en.wikipedia.org/wiki/Lionel_Messi"]
});
```

### [使用 ToolCall 调用](/oss/langchain/tools)

我们也可以使用模型生成的 `ToolCall` 来调用工具，在这种情况下将返回一个 @[`ToolMessage`]：

```typescript
// 这通常由模型生成，但为了演示目的，我们直接创建一个 tool call。
const modelGeneratedToolCall = {
  args: {"urls": ["https://en.wikipedia.org/wiki/Lionel_Messi"]},
  id: "1",
  name: tool.name,
  type: "tool_call",
}

await tool.invoke(modelGeneratedToolCall)
```

## 链式调用 (Chaining)

我们可以通过首先将工具绑定（bind）到一个 [工具调用模型](/oss/langchain/tools/)，然后调用它来在链中使用我们的工具：

```typescript
// @lc-docs-hide-cell

import { ChatOpenAI } from "@langchain/openai"

const llm = new ChatOpenAI({
  model: "gpt-4o",
  temperature: 0,
})
```

```typescript
import { HumanMessage } from "@langchain/core/messages";
import { ChatPromptTemplate } from "@langchain/core/prompts";
import { RunnableLambda } from "@langchain/core/runnables";

const prompt = ChatPromptTemplate.fromMessages(
  [
    ["system", "你是一个乐于助人的助手。"],
    ["placeholder", "{messages}"],
  ]
)

const llmWithTools = llm.bindTools([tool]);

const chain = prompt.pipe(llmWithTools);

const toolChain = RunnableLambda.from(
  async (userInput: string, config) => {
    const humanMessage = new HumanMessage(userInput);
    const aiMsg = await chain.invoke({
      messages: [new HumanMessage(userInput)],
    }, config);
    // 提取工具调用并将它们批量发送给工具执行
    const toolMsgs = await tool.batch(aiMsg.tool_calls, config);
    // 将工具执行结果发回模型以获取最终答案
    return chain.invoke({
      messages: [humanMessage, aiMsg, ...toolMsgs],
    }, config);
  }
);

const toolChainResult = await toolChain.invoke("['https://en.wikipedia.org/wiki/Albert_Einstein','https://en.wikipedia.org/wiki/Theoretical_physics']");
```

```typescript
const { tool_calls, content } = toolChainResult;

console.log("AI 消息", JSON.stringify({
  tool_calls,
  content,
}, null, 2));
```

## 代理 (Agents)

有关如何在 Agent 中使用 LangChain 工具的指南，请参阅 [LangGraph.js](https://langchain-ai.github.io/langgraphjs/how-tos/#tool-calling) 文档。

---

## API 参考

有关所有 Tavily Extract API 特性和配置的详细文档，请访问 API 参考：

[docs.tavily.com/documentation/api-reference/endpoint/extract](https://docs.tavily.com/documentation/api-reference/endpoint/extract)

## 相关

* [工具文档](/oss/langchain/tools)