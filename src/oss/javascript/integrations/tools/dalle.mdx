```typescript
/* eslint-disable no-process-env */
import { DallEAPIWrapper } from "@langchain/openai";

const tool = new DallEAPIWrapper({
  n: 1, // 默认值：生成图片的数量
  model: "dall-e-3", // 默认值：使用的模型版本
  apiKey: process.env.OPENAI_API_KEY, // 默认值：OpenAI API 密钥
});

// 调用 tool.invoke 方法传入文字描述（Prompt），生成图片，并获取图片 URL
const imageURL = await tool.invoke("a painting of a cat");

console.log(imageURL);
```

## 相关内容

- 工具 [概念指南](/oss/langchain/tools)
- 工具 [操作指南](/oss/langchain/tools)