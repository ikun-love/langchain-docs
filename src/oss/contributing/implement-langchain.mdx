---
title: 实现 LangChain 集成
sidebarTitle: 实现
---

集成包（Integration packages）是用户可以在其项目中安装使用的 Python 包。它们实现了遵循 LangChain 接口标准的一个或多个组件。

LangChain 组件是 [`langchain-core`](https://github.com/langchain-ai/langchain/tree/master/libs/core) 中基类的子类。示例包括 [聊天模型](/oss/integrations/chat)、[工具](/oss/integrations/tools)、[检索器](/oss/integrations/retrievers) 等。

您的集成包通常需要实现上述至少一个组件的子类。请展开下面的标签页以查看每个组件的详细信息。

<Tabs>
    <Tab title="聊天模型">
        聊天模型是 @[`BaseChatModel`] 类的子类。它们实现用于生成聊天补全、处理消息格式化以及管理模型参数的方法。

        <Warning>
            聊天模型集成指南目前仍在编写中（WIP）。在此期间，请阅读 [聊天模型概念指南](/oss/langchain/models) 以了解 LangChain 聊天模型的工作原理详情。
        </Warning>
    </Tab>
    <Tab title="工具">
        工具主要用于以下 2 种方式：

        1. 定义一个“输入模式”（input schema）或“参数模式”（args schema），并将其与文本请求一起传递给聊天模型的工具调用功能，以便聊天模型可以生成一个“工具调用”，或者生成要调用该工具所需的参数。
        2. 接收上述生成的“工具调用”，执行某些操作，然后返回一个响应，该响应可以作为 `ToolMessage` 传递回聊天模型。

        `Tools` 类必须继承自 @[`BaseTool`] 基类。该接口有 3 个属性和 2 个方法需要在子类中实现。

        <Warning>
            工具集成指南目前仍在编写中（WIP）。在此期间，请阅读 [工具概念指南](/oss/langchain/tools) 以了解 LangChain 工具的工作原理详情。
        </Warning>
    </Tab>
    <Tab title="检索器">
        检索器用于根据查询从 API、数据库或其他来源检索文档。`Retriever` 类必须继承自 `BaseRetriever` 基类。

        <Warning>
            检索器集成指南目前仍在编写中（WIP）。在此期间，请阅读 [检索器概念指南](/oss/integrations/retrievers) 以了解 LangChain 检索器的工作原理详情。
        </Warning>
    </Tab>
    <Tab title="向量存储">
        所有向量存储都必须继承自 @[`VectorStore`] 基类。该接口包含在向量存储中写入、删除和搜索文档的方法。

        请参阅 [向量存储集成指南](/oss/integrations/vectorstores) 以获取实现向量存储集成的详细信息。

        <Warning>
            向量存储集成指南目前仍在编写中（WIP）。在此期间，请阅读 [向量存储概念指南](/oss/integrations/vectorstores) 以了解 LangChain 向量存储的工作原理详情。
        </Warning>
    </Tab>
    <Tab title="嵌入模型">
        嵌入模型是 @[`Embeddings`] 类的子类。

        <Warning>
            嵌入模型集成指南目前仍在编写中（WIP）。在此期间，请阅读 [嵌入模型概念指南](/oss/integrations/text_embedding) 以了解 LangChain 嵌入模型的工作原理详情。
        </Warning>
    </Tab>
</Tabs>